# UI.md — Interface conventions

**Status:** Normative conventions, created 2026-08-16 (work-management plan, Step 0). Applies to every screen from Phase 1b onward; retro-applied to the six Phase-1 pages during 1b.
**Amended 2026-08-17** — §9 and §10 rewritten as the normative visual system: tokens, type scale, spacing, radii, elevation, motion, the semantic colour map for every enum, the entity-colour algorithm, the component catalogue, dark-mode rules, the accessibility checklist, and the rule that new UI uses tokens and components only (with grep tripwires as enforcement).
**Amended again 2026-08-17 (set B + reconciliation)** — the project tree, files, members, settings and account screens landed, and the whole app was then reconciled against this document: §10.10 gains `Timeline`, `ProgressMeter` and `EntityTile`; §10.15 is new and records the six recurring screen patterns every route must now use; §10.4 gains the row-cue token; §10.14 records the set-B contrast rows. Three tokens moved as a result of that audit and are noted where they live.
**Amended 2026-09-06 (⌘K, with entity rows).** The palette carries `shouldFilter={false}`, and that is not a detail: cmdk's default both filters by fuzzy score AND re-sorts the DOM by it, so a `ts_rank_cd` ranking handed to it is not a ranking any more. Turning it off makes NAV matching the component's own job, which it does with an accent-insensitive SUBSEQUENCE test so "prj" still reaches Projects — a plain `includes` would have been a silent downgrade. Every entity row's `value` is `type:id`: cmdk tracks selection as a single string and marks every item whose value equals it, so two rows sharing one both light up and Enter fires whichever is first in the DOM. The search is debounced 200 ms, guarded by a monotonic ticket so a slow answer to an old query cannot overwrite a fast answer to a new one, and cleared when the palette closes — as a render-time state adjustment rather than in `onOpenChange`, because ⌘K toggles `open` in the shell directly and never goes through that handler.

**Amended 2026-08-17 (release gate)** — §10.14 now requires the browser visual sweep (`e2e/visual.spec.ts`, `RUNBOOK.md` §7) for every visual change: the contrast table proves the palette, only the sweep proves the pages.
**Amended 2026-08-17 (craft refinement)** — the app was functional and consistent and still did not read as a product, for three reasons the founder named: every editable row rendered an always-visible input, so pages that are content looked like data entry; a solid red `Delete` was repeated on every row of every table; and the raw OS file input showed inside the upload target. Three patterns fix all three and are **normative from this date**: §5.11 `InlineEdit` (a value is text until you edit it), §5.12 `RowActions` (the row's verbs live in a `⋯` menu, and destructive weight is spent only on the confirm's "Yes"), §5.13 `FileDropField` (the native input is `sr-only`, never `hidden`). §5.14 adds the one disclosure and §5.15 the whole-page state. §10.15 grows from six recurring patterns to nine, §10.13 gains four tripwires, and §10.14 folds the craft audit (`e2e/audit.ts`) into the gate so none of the three can regress silently.
**Companion docs:** `PLAN.md` (phase bodies name the screens), `ARCHITECTURE.md` (ARC-15 UI kit, ARC-17 DnD, ARC-18 freshness, ARC-19 rich text), `AUTHZ.md` (what a screen may show is a permission question, never a UI one), `TENANCY.md` §7.2 (portal projections), `DATA_MODEL.md` (vocabulary).
**Evidence:** `docs/research/2026-08-16-work-management-synthesis.md` §5 (complaint corpora → UX rules), `…-plan-draft-ux-product.md`, `…-reviews.md`.

This document is the **source of truth for how Fortleva looks and behaves**. A build session follows it verbatim; deviations are amendments here, dated, not local exceptions. Where a rule conflicts with a data-layer rule (visibility, scoping, permissions), the data layer wins and the UI conforms.

Vocabulary is law (DATA_MODEL §1): **Tenant, Member, Client, Contact, Platform**. A `WorkItem` is called a **"Task"** in the UI; levels are **Epic → Task → Subtask**. Never "issue", "ticket", "card", "story" in user-facing strings.

---

## 1. Purpose and scope

- **Covers:** the 15 UX rules, information architecture (member app, mobile, portal), the shared component vocabulary, the keyboard map, drag-and-drop and optimistic-update rules, i18n and formatting, accessibility, density and visual language, portal-specific rules, and the deliberate omissions.
- **Does not cover:** what a principal may *see* (AUTHZ/TENANCY), schema (DATA_MODEL), copy per locale (`messages/{sv,en}.json`).
- **Enforcement:** each rule names its tripwire — an ESLint rule, a component contract, an E2E test, or a review checklist item in PLAN §2 (definition of done). Rules without a tripwire are review items.

---

## 2. The fifteen UX rules (normative)

| # | Rule | Concretely | Tripwire |
|---|---|---|---|
| 1 | **Latency is a feature.** | Every mutation is optimistic (§7). Navigation to an item is a side-peek (`?item=ACME-12`), not a page load. Perceived < 100 ms on board drag, < 50 ms on inline property change. No modal for create. | E2E: drag reflow before server round-trip; lint: no `<Dialog>` in a `create*` component |
| 2 | **Title-only creation.** | `C` anywhere, `+` in any list/column. One text field, Enter creates and focuses the next; `⌘⇧Enter` create-another with the same properties; `⌘Enter` create-and-open. Defaults come from context (column ⇒ state, group ⇒ assignee/epic, project ⇒ visibility). | E2E: keyboard-only create; review: create form has one required field |
| 3 | **Inline edit everything.** | A record property is an `<InlineEdit>` (§5.11) — text at rest, the real control on click / Enter / F2; a *work-item* property is the `<PropertyPicker>` popover (§5.2, Phase 2) with single keys `S A L P E D V M T X`. **No Save buttons anywhere** — a change is committed on select/blur, undoable via toast. An always-mounted input for a value nobody is editing is the defect this rule exists to prevent. | Lint: no button whose label key ends in `.save` outside `/settings/*` and auth pages; audit: no visible input inside a resting table row |
| 4 | **Drag with a keyboard twin.** | Desktop: Pragmatic DnD, drop indicator + auto-scroll. Keyboard/mobile: "Move to…" (menu, palette, long-press sheet). `rank` never rendered. | E2E: keyboard-only move; grep: `rank` absent from any client component's JSX |
| 5 | **One board per project; columns are states; position is priority.** | No swimlane settings, no per-team boards, no board-only fields. Group-by (assignee/epic/priority/label) is a *display* transform of the same view. Portal never sees state names, only categories. | Review; portal forbidden-columns grep (state names) |
| 6 | **One universal `<WorkItemView>`.** | Home, backlog, board, project list, portal task list are the same component with different config (§5.3). URL-addressable via `nuqs`. No `SavedView` table in v1. | Import graph: no second list/board implementation |
| 7 | **⌘K first, single keys second, shortcuts shown everywhere.** | Every action reachable from the palette. Tooltips carry the key. `?` opens the overlay. Palette rows show their key. | Review: new action ⇒ palette entry + tooltip key |
| 8 | **Home is the home page.** | `/home` = assigned (overdue / today / next 7 d), waiting on client, triage count, inbox top-5, timer slot, this-week hours (own). Post-login lands here; `/dashboard` is only the workspace picker for > 1 membership — the account menu offers it only above one, and each row POSTS (`switchWorkspaceAction` writes `Session.activeTenantId`; a `<Link>` would switch on Next's prefetch). **The active workspace is a SESSION pointer, so it is one workspace per browser, not per tab** — and every other tab of that session is then showing one workspace while everything it posts resolves another. Those tabs are FENCED rather than left to write into a workspace nobody is looking at: `<WorkspaceWatch>` (shell, every authed page) hears the switch over a `BroadcastChannel` and puts up an undismissable modal offering the two answers that resolve the split — reload into the workspace the session is in, or put the session back into this one. See `src/lib/workspace-watch.ts` for the protocol and for why the fence is in the browser: cookies are per ORIGIN, not per tab, so nothing the server reads distinguishes a stale tab, while a shared session means a shared profile means a channel that reaches every tab that has the problem. | E2E: login → `/home`; the offer, the switch and the stale-tab fence, `account.spec.ts` |
| 9 | **The timer is a personal tool.** | Pill on every page; one tap to stop; starting another auto-stops the running one with an undo toast; 8 h nudge, 12 h auto-stop → needs review; `1h 30m` / `90m` / `1,5` accepted; raw seconds preserved. Never shown to peers. *Amended 2026-08-20 (industry sweep):* the pill quick-start takes a bare description (= instant task, non-billable until a project is attached) with `@project` / `#task` autocomplete; a recents dropdown offers **one-click continue** (copies task/project/agreement/type/billable — the category's most-loved interaction); a global start/stop shortcut exists (palette-discoverable, rule 7); elapsed time ticks in the **browser tab title**; **copy-last-week copies rows, never hours** ("copy with durations" is the explicit secondary action); overlapping entries are **allowed and badge-flagged, never blocked** (strict blocking is the tenant opt-in); shift clock-in/out + breaks live on `/time` and in the pill menu — a break auto-stops the running timer (undo), auto-closed shifts are visibly provisional until confirmed. | 2T tests (PLAN §3) |
| 10 | **Visibility is loud, explicit, inherited, previewable.** | Two-token badge on every class-B row; composer asks on create in portal-enabled projects; children default from parent and cannot exceed it; two-mode comment composer; "View as client" banner; warning when mentioning/assigning a Contact on an INTERNAL item. | "No INTERNAL fact to a Contact" fixtures; view-as byte-identity test |
| 11 | **Progress = human health + machine numbers.** | `ProjectUpdate.health` chosen by a person, metrics filled by the system; immutable after publish; client sees one screen + an action-items inbox. | Update immutability trigger test |
| 12 | **Digest, not firehose; explicit over silent.** | Only assignment (debounced) and mention email instantly; everything else coalesces/digests. Archive is a visible action with restore; filter chips always visible; locked entries say why (*2026-08-20:* grey + lock icon + tooltip naming the `lockedReason`; unlock is an explicit admin action that never mutates the issued invoice); rate changes ask "from today / from date / all unbilled" and always state "past entries unchanged; use Reprice to correct history". | Kind-catalog audience test; review |
| 13 | **Opinionated defaults with escape hatches.** | 5 priorities, ~5 states from a preset, hours as estimate unit, single assignee, depth ≤ 3. The only "process" choice is a project template. | Review; no settings page for card styles / process |
| 14 | **Cost is sensitive.** | Employee: own hours. Manager: hours + bill rates + budgets. Finance: cost + margin behind ✦ step-up. Nothing per-member in the portal. No presence, activity, or leaderboard UI anywhere (never-list, SECURITY.md). *2026-08-20:* team time surfaces aggregate **closed** shifts/entries only for members other than the viewer — no live clock state, no on-shift indicator; the agreement picker shows names, never rates, without `rate:view_bill`. | Forbidden-columns grep; CSV default omits cost |
| 15 | **Empty states have a verb; density compact; every entity has a chip; destructive actions confirm inline; mobile is bottom tabs.** | See §5.7–5.9, §11, §4. | Review |

---

## 3. Information architecture — member app

### 3.1 Left rail (desktop) — fixed order, no reordering, module-gated items hidden (not disabled) when the entitlement/preference is off

| Rail item | Route | Notes |
|---|---|---|
| Home | `/home` | Rule 8. *2026-08-21 (landed):* the **"Your time" strip** — two `MetricTile`s (this week / today, the member's OWN finished hours; a running timer's elapsed is added live from its server start and counted into today / this week exactly where its stopped row will land — by its START local date) and the **timer slot** (a Running tile ticking, or the same tile as a dashed ghost holding the "Start a timer" verb into the quick start); shown only to a member who can track time (`canTrackTime`, decided before any time service runs); no header verb — the rail item and the tiles already lead to `/time`. All live clocks outside the pill share `useServerNow` (`src/components/shell/use-server-now.ts`). *2026-09-17 (slice 23, landed):* **the queue** — "Your queue", the member's OPEN (Backlog / To do / In progress) tasks assigned to them, not archived, not in an archived project, across every project their scope still reaches (`listMyWork`, scope composed into the query: an assignment outlives access, the title must not), in **four** groups by due date against the member's server-resolved today — **Overdue · Due today · Next 7 days (tomorrow → today + 7) · Later or no date** (`src/lib/my-work-queue.ts`). The fourth group is a deliberate extension of rule 8's three: most tasks carry no date, and a queue of the three alone told a member with a dozen tasks in progress that they had nothing to do. Order: soonest due, undated last, then URGENT → NONE, then key. A row is ONE link to the task's peek over its backlog (the inbox's address for a task): state icon · key (≥ sm, at least 10ch — short keys align their titles; a longer key pushes its own) · title · project (≥ sm) · state name (≥ md; spoken below it — To do and In progress share an icon) · priority glyph with a spoken label · the due day (none in Due today; the danger tone on Overdue, whose heading also says it in words). Group headings are h3s carrying their count, not landmark regions. The card is a server component; since slice 24 (below) the rows are a client list (`home/queue-rows.tsx`) carrying `J K` and `T`; capped at 100, and then a sentence ABOVE the groups says so (the cap cuts from the end, so the last group drawn may be short and a later one missing; the counts are of the rows shown). Drawn only for a member with `work_item:view` AND `project:view` — a row names the project and links into it, so without the page's own code there is no queue (the read answers `null`, the strip's rule); nothing assigned is `empty` with the verb *Go to projects*. **The inbox card** — ABOVE the queue (at most five rows; below a long queue it would sit two phone screens down) — the newest five live unread notifications (`inboxGlance`: the Unread tab's predicate and its scope-filtered subjects) with the unread count and *Open inbox*; drawn only while it has rows (the rail badge already says "nothing"). **Not here, on purpose:** "waiting on client" and the triage count — nothing in 2W writes a contact assignee or a `triageStatus`, so both would be cards whose number is always zero; they arrive with Phase 3's writers. *2026-09-17 (slice 24, landed):* **row verbs** — `J K` roving focus on the rows (the row's link is the focus target) and `T` / a **start-stop button** per row for a member who tracks time, §5.4 "Timer on the queue row", §6 `home`. |
| Clients | `/clients`, `/clients/[id]` | Tabs: Overview · Projects · Contacts · Files · Agreements (2T — *added 2026-08-20*: `Service` rows presented as "Agreements"/"Avtal", their rate cards, and a consumption strip "X h this period"; rates masked without `rate:view_bill`) · Vault (3V) · Assets (3V). |
| Projects | `/projects` (grouped by client), `/projects/[key]/…` | Tabs in this order: **Board · Backlog · Timeline · Updates · Time · Files · Team · Portal**. Board/Backlog need `work`; Time needs `time` (the finance-gated **Money** page `/projects/[key]/money` lives under the Time tab as a sub-view, shown only with `rate:view_bill`/`rate:view_cost` — `PLAN.md` Phase 2T; *2026-08-20:* the **Reports** sub-view `/projects/[key]/time/reports` — `TimeReport` list · composer with period/grouping/amounts · preview · publish with the two-token visibility badge — also lives under Time, `time_report:manage`); Updates/Portal appear in Phase 3; a tab the actor lacks permission for is hidden. |
| Time | `/time` (My Time), `/time/team` (`time:view_team`) | `time` module. *2026-08-20:* `/time` = today (clock in/out + breaks when `time.shiftsEnabled`, the **day reconciliation strip** shift − breaks − tracked = unallocated, continue-on-every-row) + the ISO week grid (`ui.weekStart`, copy-last-week rows-only) + needs-review banner (provisional auto-closed shifts, one-click fix) + overlap / missed-rast warn badges + the member's own **working-time statement** (month picker, CSV + print). `/time/team` = per-member closed-day totals by project/agreement/type over a range, Δ vs `Member.hoursPerDay`, warn-flag filters, statement export per member. *2026-08-21 (landed):* `/time` carries a **Working-time statement** card (the viewed week's month → CSV + "Print view") and a week-CSV icon in the header; **`/time/statement?month=`** is the print layout itself (shell chrome `print:hidden`, prev/this/next month, CSV, Print); CSVs are same-origin attachments from **`/time/export`** (`kind=entries` own · `kind=team` · `kind=statement[&member=]`) and **`/projects/[key]/time/export`** (entries · `kind=rollup` · `&cost=1` only in the Money view's revealed state) — headers are machine keys, numbers machine-formatted, rate/amount columns only with `rate:view_bill`, cost only through the audited ✦ reveal (rule 14). The team page's shifts table gains one trailing ghost verb per member: that member's monthly statement CSV (`time:export`). *2026-08-21 (landed, later session):* **copy-last-week** lives in the week card's header — `Copy last week` (outline) copies last week's **rows** onto the same weekdays with an **empty** duration the member fills in; `Copy with durations` is the one item behind the caret (the explicit secondary, never the default); rendered only when last week has rows and past the notice gate; idempotent (a row already on that day is skipped); a DURATION row (anchored at midnight, no clock) neither flags nor blocks an overlap. **Split…** (same day, later): a finished, unlocked row's menu verb, answered by ONE in-place form under the table (the first part's length, default half) — the first part keeps the row, the second is a new row with the same task and rate; re-home or reprice afterwards, as an ordinary edit. |
| Search | `/search` | Core. *2026-09-06 (landed):* the query is a SERVER param (`?q=`) and the page re-renders for it — the opposite of the backlog's chips, because there is no client-side copy of the corpus to filter; the box debounces 250 ms and `replace`s rather than pushes, so refining a search is one view and not twelve. **FOUR states, and they are four because collapsing any two lies to somebody**: idle (nothing typed) · too-vague (the query parsed to no lexemes — "the"/"och" are stop words, and answering that with "no results" says the workspace is empty when it is the query that was) · nothing-matched · results. Rows are grouped by type for the heading only; the order inside is the SERVER's (`ts_rank_cd`, then recency) and nothing re-sorts it. Each row's href is resolved from the LIVE source in the hydrate pass, never parsed out of the `KEY-12` subtitle — the index freezes that label at feed time, so a project rename would otherwise break every link. `G S`. |
| Inbox | `/inbox` | Core; unread badge. *2026-09-06 (landed):* four buckets as a `TabNav` — **Unread · All · Snoozed · Archived** — each a real server query on `?filter=`, not a client predicate (the opposite of the backlog's chips, and for the reason that decides it there: the backlog already holds the whole project in the browser, a bucket here is a different `WHERE` against a paged table). One row per notification: kind label, the subject's title as a link to its peek, relative time (§9 "relative only for activity/inbox"), an unread dot **plus** the label's weight. Verbs in the row's `⋯` menu — mark read/unread, snooze (3 h · tomorrow morning · next Monday, computed in the BROWSER because `Member.timezone` is routinely empty), archive/restore — plus **Mark all as read** on the Unread bucket. Paging is a keyset on the row **id** (UUIDv7), never on `created_at`: rows emitted in one transaction share that stamp to the microsecond and a JS `Date` cursor rounds it down, which silently dropped whole batches at a page boundary. **The list is ungated; the SUBJECT is not** — reaching your own inbox needs no permission, but a title is a work item's and a name is a project's, so `work_item:view` / `project:view` gate the resolution and an unpermitted, out-of-scope, deleted or unknown entity all render identically: the kind, no title, no link. `G I` ships (a nav-registry `goKey`, so the palette and the `?` overlay get it for free); the `inbox`-scope single keys below do not — see §6. |
| Vault | `/vault`, `/expirations` | `vault` module. |
| Reports | `/reports` | Phase 6. |
| Settings | `/settings/{general,members,roles,preferences,export,labels,templates,workflow-presets,rates,time,notifications}` | Only pages the actor can act on. *2026-09-06 (landed):* **`/settings/notifications` is the one page here with NO permission gate** — every other Settings page administers the workspace and is hidden without its code; this one administers one person's own mail, and no seat in this product decides on someone else's behalf whether they are emailed. It ships exactly what is wired: the **email level** (Everything · Work involving me · Only mentions · Nothing), honoured per KIND by `notify.emit` through the catalog's level ladder, and the 2T **weekly time reminder** opt-in. `inAppLevel`, digest cadence/hour/weekday and quiet hours are columns nothing reads until Phase 5 and are deliberately absent — `inAppLevel` most of all, because the inbox is where an assignment is found. |

The rail is open by default from `md` (768 px) up and collapsible to icons by the member at every width from there (its state is per browser); below `md` the bottom tabs replace it. *(Corrected 2026-09-16: this line said the rail collapses to icons below 1280 px. It did until `c2c25c5` (the design-token restyle) replaced `w-12 xl:w-56` with an open-from-`md` rail, and column priority went on assuming the icon rail; §10.12.)* Project tabs become a horizontal scroller. `/projects/[key]` redirects to Board when `work` is on, else Timeline.

### 3.2 Global chrome (every authed member page)

- **⌘K palette** (`cmdk`): recents → `KEY-123` jump → per-type capped search (tasks, projects, clients, files, credentials by name) → actions ("Start timer on…", "Move to…", "New task in…"). Never returns a row the actor could not open.
- **Timer pill** (2T): header slot, desktop right of breadcrumb; mobile above tabs. Shows task title, elapsed with skew-corrected local tick, stop button; click → jump to the item.
- **`?` overlay**: the keymap of §6, filtered to the current scope.
- **`C`** creates a task in the current context (project if inside one, else asks project first via picker).
- Toasts (`sonner`) bottom-right desktop, top mobile; every toast with an undo path has one.

### 3.3 Mobile (< 768 px)

Bottom tabs **Home / Board / Timer / Inbox / More**. Board is a single-column list grouped by state with state chips (tap chip → "Move to…" sheet). No touch drag (ARC-17). `More` = rail items not in tabs. Long-press on any task = action sheet (state, assignee, timer, visibility). Web Push opt-in arrives Phase 5. *(2026-08-20, decision 15 / ARC-25)* **Installable from 2T:** the member app ships a manifest (`display: standalone`, `start_url: /home`, maskable icon, `theme_color`/`background_color` = the `--background` token per theme) and a pass-through service worker — **no offline, no install-only screens, no "open in app" banners** beyond the browser's own prompt. Installed and browser tabs render the same UI; `display-mode: standalone` may adjust chrome only (safe-area insets, no duplicated back affordance), never anything a permission or visibility rule touches. The portal gets its own manifest at Phase 3 (CP3); the ops host none.

---

## 4. Information architecture — portal

- `/portal` = **Action items first** (approvals pending, tasks assigned to you, questions awaiting reply, credential submission requests), **then project cards** (name, health chip, next milestone, last update age). Only projects with `portalEnabled` and the contact's client.
- `/portal/projects/[key]` = **one screen, this exact order** (PLAN §3 Phase 3):
  1. Header — project name, health chip, "Phase: Design · Next milestone: Launch due 12 Sep", milestone progress bar.
  2. Action items for this project.
  3. Latest published update.
  4. Timeline (derived: updates, milestone dues/completions, version ships/approvals, deliverable versions, approval decisions).
  5. Shared tasks grouped by **category** (only if the project enables the task list; no estimates, priorities, state names, assignee names).
  6. Files & deliverables (approve / request changes).
  7. Hours & retainer (only when `hoursSharingMode ≠ NONE`, CONTACT_PRIMARY only).
  8. Requests (create + own list with category status).
- `/portal/files`, `/portal/company`, `/portal/share/[token]` (3V), `/portal/projects/[key]/submit-credential` (3V).
- No portal left rail, no tabs, no board unless the project enables it (default off), no settings beyond profile/notification email toggles.
- Exhaustive contact actions: view; approve / request changes; complete an own-assigned task; comment (forced CLIENT_VISIBLE); create a request; download; submit a credential; open a share link. **Nothing else** — a portal screen with another button is a bug.

---

## 5. Component vocabulary (`src/components/ui` primitives + `src/components/*` composites)

### 5.1 `<EntityChip>`
One chip component for Member, Contact, Client, Project, Task (`ACME-12`), Milestone, Label, ProjectVersion, Document. Avatar/icon + name + optional secondary; hover card with the entity's summary; click = side-peek or navigate. Contact chips in the member app carry a small "client" glyph. In the portal, member chips are never rendered (v1) — the composite refuses to render `Member` when the principal is a Contact.

### 5.2 `<PropertyPicker>`
Popover with type-ahead list; single key opens it when the item is focused; Esc closes without change; Enter/click commits (optimistic). Keys and pickers:

| Key | Property | Picker | Notes |
|---|---|---|---|
| `S` | State | states of the project grouped by category | shows category dot; TRIAGE hidden unless item is in triage |
| `A` | Assignee | members in scope, then contacts (portal-enabled projects only) | choosing a contact warns if item is INTERNAL and offers to make it CLIENT_VISIBLE — **members shipped 2026-09-13** (slice 7): every ACTIVE member of the tenant today, exactly as the backlog's cell and the board's lanes list them; narrowing all three to the project's scope is recorded in PLAN §0, and contacts and the warning are Phase 3's |
| `L` | Labels | tenant labels, multi | internal-only; ONE label per pick, toggled; "Create “…”" from the typed text under `label:manage` — **shipped 2026-09-15** (slice 12) |
| `P` | Priority | NONE LOW MEDIUM HIGH URGENT | enum order — **shipped 2026-09-13** (slice 6) |
| `E` | Estimate | text `2h`, `1,5`, `90m` → minutes | typed text → a derived row in the shared grammar; a typed `0` or "Remove estimate" clears — **shipped 2026-09-13** |
| `D` | Due date | calendar (grid starts on `ui.weekStart`, default Monday; ISO week numbers when `ui.showIsoWeek`); tokens today / tomorrow / next week; a typed ISO date | "next week" = the first day of the next grid week, each token row showing its resolved date; token values are ids, never dates; "today" is the member's day in their zone — **shipped 2026-09-13** |
| `V` | Visibility | two tokens (§5.5) | refuses INTERNAL when a child is CLIENT_VISIBLE; explains — never optimistic (§10.4) — **shipped 2026-09-13** (slice 7) |
| `M` | Milestone | project milestones by rank | a CANCELLED phase is not a target; the item's own is always a row — **shipped 2026-09-15** (slice 11) |
| `T` | Timer | start/stop on this item | not a picker: the start/stop control in the panel's header — **shipped 2026-09-17** (slice 19), §5.4 "Timer" |
| `X` | Select | toggles multi-select for the bulk bar | on the BACKLOG's focused row, never in the panel — **shipped 2026-09-15** (slice 14); see §6's `backlog` row |

**The shipped `<PropertyPicker>` contract (slice 5 on 2026-09-12, slices 6 and 7 on 2026-09-13, slices 11 and 12 on 2026-09-15 — `src/components/semantic/property-picker.tsx`).** `S A P E D V M L` use it (that is also their rail order, and so the `?` overlay's). Slice 6 added two optional seams (`derive`, `footer`); slices 7 and 11 needed neither; slice 12 added a third, `selected`, and nothing else. What slice 7 adds is two islands under the rules below: `A` lists the tenant's ACTIVE members in the list surfaces' order with the current one checked in place, the "Unassigned"/"Unassign" pair following the empty-property rule, and no contacts (Phase 3 owns `assigneeContactId` and the "make it client-visible?" warning); `V`'s trigger rest state IS the `<VisibilityBadge>`, so the editable chip and the read-only chip are one silhouette, its refusal is the database's own (`HAS_VISIBLE_CHILDREN`, naming what to make private first), and it is the one island that is never optimistic. **Slice 11 adds `M`** and needs no new seam either: the rows are the project's milestones by RANK — the timeline's own order, so the picker and the timeline can never disagree about what comes after what — with the current phase checked in place and the "No milestone"/"Remove from milestone" pair following the empty-property rule; the dated ones carry their date in `meta`, which is what tells "Sprint 3" from "Sprint 4"; each row's glyph is the milestone's own `STATUS_MAP.milestoneStatus` icon, and the trigger draws the current one's. Which phases may be CHOSEN is one rule, `milestonePickerTargets` (`@/lib/work-view`, unit-tested there): a CANCELLED phase is not a target, and the item's own is always a row — non-selectable when it is one of those, the `S` rule verbatim, because a picker that cannot show what the item IS is broken. The row is a row only where there is a phase to show or to reach: the rail draws it when the item HAS one or the member can edit and the project HAS one (`milestones` is empty for a member without `work_item:edit`, so a viewer sees the row only on a task already filed), and the island degrades to plain text — no control, no `M` — whenever nothing is choosable, which covers a project whose phases are ALL cancelled with the task under none. That question is asked of the CANONICAL value, never the optimistic one: whether a property is a control is structure, and structure may not follow a pick the server has not answered. The change is ROUTINE (an activity row, no audit) though `milestoneId` is portal-safe, so on a shared task the client reads that row — and it carries the phase's ID, never its name, because a name written into a client-visible row is a copy that outlives every later decision about the phase's own visibility; the READ resolves it (`activity.ts`), which under the Phase 3 contact principal is RLS's answer and not ours — and `oldRef`/`newRef` are on `PORTAL_FORBIDDEN_COLUMNS` besides — a reminder, not the guarantee: that scan reads only files named exactly `portal.ts` and matches the camelCase identifier, so a raw `old_ref` or a `portal/history.ts` would walk past it. The database refuses; the grep reminds. **Slice 12 adds `L`, the first MULTI property, on the single-pick contract:** a pick still commits ONE row and closes, and what that pick means is a TOGGLE of that one label — one label per round trip, and the service answers with the whole list as it then stands, so two members labelling one task never overwrite each other's adds. The rows are the vocabulary the task may draw from (tenant-wide labels and its project's own), in ONE order, by name, and every applied row is checked in place through the picker's third seam, `selected` (its "(current)" words read "(applied)"). `value` is null for a set, so nothing is seeded on open, a bare Enter is INERT (the picker stays open — there is no current row for it to re-pick) and the member steers or types first: a checked row that Enter re-picked would be a REMOVAL, not a no-op, which is why the empty-property rule's "current row first" and its "No …"/clear pair cannot apply to a set — every applied row is its own way off. The typed row is "Create “<text>”" (the `derive` seam), offered only to a member who holds `label:manage` and only while no row has that name under the one key the database's index uses (`labelNameKey`: case, not diacritics — "Café" and "Cafe" are two words, and the client should not hide a word the server would accept; the one exotic exception, a word-final Greek sigma on a libc collation, is recorded on `labelNameKey` and accepted); it runs ONE transaction that creates the tenant-wide label, audits `label.created` and puts it on this task, so a member who may coin a word but may not edit this task creates nothing. The rows are the vocabulary PLUS any applied label it does not carry yet, so a label coined a moment ago is a row before the refresh delivers it. The applied set is part of the picker's highlight BASIS: a steered row whose membership flips underneath the member is re-seeded, because a pick there is a toggle and would have gone the other way. One order everywhere (`compareLabelNames`), never a collation: CI's C.UTF-8 and the browser's ICU disagree. Filing is routine (an INTERNAL history row — `labels` is not portal-safe, and a label is a word the client never sees); coining is not. The chips are neutral `Badge`s: `Label.color` is a token name nothing writes yet, and a chip is identity, never status (§10.4). The row exists where there is a label to show or a word to reach (the task's own, a vocabulary to pick from, or the right to coin one), degrades to text otherwise, and the gate is asked of the canonical props as `M`'s is. Not yet: a colour, renaming or deleting a label (its settings surface), project-scoped labels (the schema allows them; nothing creates one, and the service already refuses another project's), group-by label, a label filter. **Labels on the board card and the backlog row shipped 2026-09-16 — §5.3a below.** Settled, do not re-litigate:

- `open` / `onOpenChange` / `value` are **required controlled props** — the property's single key must be able to open it, so the state cannot live inside the component.
- A **modal** Popover. The item peek's overlay mounts `RemoveScroll` with `shards`, which preventDefaults every wheel event outside the lock, so a non-modal popover inside the peek would have a list a mouse could not scroll.
- `shouldFilter={false}` with the picker owning matching (`matchesQuery`, shared with the palette), because cmdk's scorer also **re-sorts** and a state picker's group order is the project's `WorkflowState.rank`. With filtering off cmdk's own `CommandEmpty` can never fire — hence `CommandEmptyState`.
- `vimBindings={false}` (cmdk's Ctrl+k means "previous item" and shadows the global ⌘K on Windows/Linux — since slice 6 the `Command` wrapper defaults it false for every caller, and `keymap.test.ts` pins that only the wrapper imports cmdk) and `disablePointerSelection` (an idle cursor must not silently re-highlight the row Enter then commits).
- A `CommandItem`'s `value` is an **id, never a label**: cmdk tracks selection as one string, so two rows sharing a value both light up and Enter fires whichever is first in the DOM.
- The focus ring is `-outline-offset-2`, **negative** — the peek is `overflow-y-auto` and clips a positive one. Never `forceMount` inside the sheet (`hideOthers` would leave the node permanently `aria-hidden`).
- **No tooltip on the trigger**, deliberately: Radix opens a tooltip on focus and returns focus to the trigger when the picker closes, so the tooltip would be open the moment the popover shuts and would eat the member's next Escape. The key is advertised by the `?` overlay, the ⌘K row and `aria-keyshortcuts`.
- `S`'s rows come from `statePickerTargets` (`@/lib/work-view`), whose test ids are numbered over the FULL state list so they never depend on who is looking; its targets are filtered by `enterableStates`, which **always includes the item's current state**, non-selectable when it is not a legal target — a picker that cannot show what the item IS is broken for an item in TRIAGE or in a gated Done under a non-approver.

- **`derive`** (slice 6) builds ONE row from the typed query — E's `90m`, D's `2031-03-14`. It renders first, ungrouped and never filtered; a fixed option with the same value is dropped; a `disabled` result counts as none (cmdk's first-row selection would skip it and Enter would commit the next row); text that is not a value returns null, so the empty state speaks (`src/lib/picker-rows.ts`).
- **`footer`** (slice 6) renders non-list content AFTER the cmdk root, inside the popover — D's calendar. After, never inside: cmdk's root `onKeyDown` owns Enter, ArrowUp/ArrowDown, Home and End for every descendant of `<Command>`, so a calendar inside it would commit the highlighted row when Enter was pressed on a day. `footer` is handed `commit`, the exact select-and-close a row runs, so there is one commit path. Its root must not shrink: the popover content is a scrolling flex column.
- **A bare Enter never commits a value the member did not choose** (slice 6; binding for `S P E D` and later `V M L`). A property that can be empty lists its current state FIRST — the value with a check, or a checked "No …" row when unset — and its clear row LAST, only when a value is set, under a DIFFERENT value from the unset row (`none` vs `clear`, both committing null): under one shared value the 12 s board poll turned a highlighted no-op into a clear beneath the member's Enter. `initialHighlight` seeds the highlight on open: `""` when the current row is the first enabled row (cmdk lights it), the current value when it is enabled but not first (`P`, `S`), and `NO_HIGHLIGHT` when it is disabled or absent (`S` on TRIAGE, or a gated Done under a non-approver) — nothing lit, Enter inert, and the first ArrowDown lands on the first legal target. *That last case REVERSES slice 5's choice to seed the first enabled row, which let a bare Enter reopen a gated task its member could not re-close.* The highlight is settled again whenever `value` or an option's value, label, keywords, group or disabled flag changes while the picker is open (`highlightAfterChange`; the query is never watched, so typing never re-seeds): a highlight the member STEERED survives while its row is still rendered, enabled, in the same group and not the old value's row — or is the typed derived row — and everything else is re-seeded (`reseedHighlight`) and counts as un-steered again. Enter is refused unless the highlight is the current value or the member has steered (ArrowUp/ArrowDown/Home/End, or typing), because cmdk re-picks its first enabled row by itself when the lit row unmounts. So a colleague's edit landing under an open picker can neither turn a bare Enter into a write nor discard the row the member deliberately steered to. Option values are ids or tokens with no whitespace — `pickerRows` throws otherwise — because the picker's reserved cmdk values (`NO_HIGHLIGHT`, the derived row's `derivedRowValue`) contain a space.
- **What is announced is less than was designed, MEASURED in Chromium against cmdk 1.1.1.** A row cmdk lights by itself (the `""` seed on open, a typed derived row, a re-pick after an unmount) is `aria-selected`, but `aria-activedescendant` stays unset or names the row lit before, and a controlled seed or re-seed never sets it (after a re-seed it keeps naming the last row the member steered to, or a node that has left the DOM); only the member's own ArrowUp/ArrowDown/Home/End or a click set it right. So no seed is announced until the member steers — a **recorded defect** (PLAN §0) whose fix is active-descendant wiring of our own. Enter acts on `aria-selected`, so it can never change what is written. Meanwhile the trigger's label speaks the current value before the picker opens, and the current row carries `labels.current` ("(current)") as sr-only words beside its aria-hidden check — both drawn by the picker itself from one comparison (since slice 7; an island hands it no check through `meta`, which is for a row's other trailing content, D's resolved dates).
- An ALWAYS-mounted sr-only `role="status"` speaks the empty text for a typed query that matched nothing (a status that appears together with its text is never announced). `labels.input` names the combobox and the listbox — cmdk names the listbox "Suggestions" otherwise. `PopoverContent` carries `gap-0`, so a `footer` sits flush, and `max-h-(--radix-popover-content-available-height)` with the content itself as the scroller: Radix flips a popover but never resizes it, so `D`'s ~450px ran off a 667px phone with its last weeks unreachable.
- The rail's islands share ONE commit path, `usePanelCommit` (`item-panel/use-panel-commit.tsx`): a transition, `useOptimistic` over required server props, a no-op guard whose basis is the newest pick IN FLIGHT while one is pending (against what is shown alone, a non-optimistic island dropped a reversal back to the unchanged chip as a no-op — slice 7's review), adopt-and-refresh on both `changed` branches and on failure, the island's always-mounted live region handed back as `status`, and a sequence token so a commit superseded by a newer pick adopts, announces and refreshes nothing when its answer lands (a superseded failure still toasts; and if the newest pick then fails after a superseded one answered, that answer's canonical value is shown and announced — changed or not, it is the newest stored value, what the item ends on). `V` alone passes `optimistic: false` (slice 7, §10.4): the chip holds the canonical prop until the canonical row is adopted, so a refused downgrade never shows a value the row never held.

`MovePicker` remains the §7.1 move twin (state **×** position) and is deliberately not folded into this. *Re-evaluated 2026-09-13, after `P E D`:* its rows are anchors ("Top of To do", "After ACME-9"), not the values of one property, so it wants no `derive`, no `footer` and no current-state row. It keeps its test ids, and inherits the wrapper's `vimBindings` default — Ctrl+K inside "Move to…" now reaches the palette, and Ctrl+J/K/N/P no longer move its highlight.

**The backlog table's S/P/E/D cells stay `InlineEdit`s** (founder decision, ratified 2026-09-13 — PLAN §0). They already write through the same four `ActionResult` actions as the panel, but the picker's trigger is `h-8` where a table row's control is `h-7` (`density="table"`), and `e2e/work.spec.ts` pins the native controls — converting them waits for a table-density trigger, in a backlog slice of its own.

### 5.3 `<WorkItemView>`
Config: `{ filters, groupBy, orderBy, layout: 'LIST' | 'BOARD', display: { estimate, labels, key, checklist, assignee, dueDate, timer } }`. Filter chips are always visible above the view (Planner's hidden filters are a top complaint); URL state via `nuqs` (`?state=…&assignee=…&group=…&layout=board`), so every view is a link. Virtualised at ~200 rows. Bulk bar appears on `X`/checkbox. The portal instance is the same component with a fixed config and a Contact principal — it renders category chips, never state names.

### 5.3a Labels on the card and the row — shipped 2026-09-16 (`display.labels`)

`<LabelChips>` (`src/components/work-view/label-chips.tsx`) is the ONE renderer of a task's labels on the two LIST surfaces, fed by ONE read: `listItems` now returns `labels` per item — in `compareLabelNames` order, grouped from a single pass over `work_item_label` beside the attachment counts, never a query per row. The chips are neutral `variant="outline"` `Badge`s, the same silhouette the rail's are, because `Label.color` is still a token name nothing writes and a chip is identity, never status (§10.4): a coloured label chip beside the visibility badge would put a second meaning on the one filled warm pill that means "Client can see".

**ONE prop, `surface`, carries every difference**, because each of them is a consequence of that single fact and a caller that could pass them separately could pass a combination neither surface wants. `LABEL_CHIP_CAP` is `{ row: 2, card: 4 }`; past the cap the remainder fold into one `+n` chip that carries the folded NAMES — as its `title` for a mouse and as sr-only words for a reader ("also Mitt and Zeta", formatted as a list in the VIEWER's locale by `useFormatter().list`, never a hard-coded comma join) — because "+2" alone tells someone a task has labels they cannot identify. Both surfaces carry an sr-only "Labels", since neither has a column header to say it for them.

**The row's chips are inside the TITLE CELL, and that is a measured decision rather than a preference.** A Labels column was built first and then measured: at the audited 1440px desktop the backlog table's `scrollWidth` already EQUALLED its `clientWidth` (1168px), and all 63px of remaining slack sat in the flexible title column — 1ch is 8.2px there, so even a 14ch column was 115px and would have pushed the trailing actions cell out of the scroll box, which the craft audit fails (`offscreenRowActions`). *(That was before column priority read the table's width and the actions column was pinned, both 2026-09-16, §10.12: the verbs now stay in view, but the width is still spoken for — a Labels column would push a narrower rung onto every laptop, or make the table scroll.)* So the chips share the title cell: `shrink-0` with a `max-w-1/2` cap, so they take their content width and the title's `w-full` `<InlineEdit>` shrinks around them, and a labelled task can never lose its title to its labels. **And the cell's flex wrapper carries `contain-inline-size` + `w-full`, which the labels e2e found rather than any reading of the CSS:** auto table layout sizes a column from its cells' MIN-content, and while it does, `max-w-1/2` has no definite width to resolve against, so the chips' FULL width became the title column's floor — two 18-character labels pushed the table 172px past its scroll box. Measured with the style injected into the live page: on every row's wrapper the table is back to 1168 = 1168 and the title column back to its old 287px, with an unlabelled row's cell unchanged; on ONE row only it still overflowed, because a column is one width. The containment takes the wrapper out of the column's intrinsic sizing (the cell's `min-w-56` is the floor again) and `w-full` gives the flex row the definite width the half-cell cap resolves against. A name chip is made shrinkable (`min-w-0 shrink` over `Badge`'s own `shrink-0`) so it TRUNCATES with a `title` rather than clipping mid-word at the cap; the `+n` chip is not, being the one thing in the group that must stay readable at any width. Nothing wraps, so the row keeps the `--row-h` the audit measures. On the card the group is its own block between the title and the meta row, wraps, and takes the full width — where the cap is a HEIGHT budget instead, a card that grew with its labels making the whole column below it scroll.

**Two consequences of the containment, both found by the fresh-agent review.** (1) The title column no longer grows to fit a title, so the title must truncate on EVERY render path: an editor's rest button always did, but a read-only `<InlineEdit>` renders `display` in a plain flex span, where a long title painted over the chips and into the State column — so the field is `w-full` and `display` is `min-w-0 truncate`, with the full title as its `title` on the read-only path, which has no button to carry one. (2) While the title IS a control the chips step aside (`has-[[data-editing]]:[&>[data-slot=label-chips]]:hidden` on the wrapper), so a 400-character title is edited in the whole cell rather than the half the chips would leave — less, since 2026-09-17, the running-timer glyph (§5.4, slice 21), which stays beside the field on the one row being timed. The chip group's styling hook is `data-slot="label-chips"`, never its test id.

**Freshness.** Labels are the first thing on a card that lives OUTSIDE `work_item`: a toggle writes only `work_item_label`, so the board's 12 s version token (`projectWorkVersion`, built from `work_item` and `workflow_state`) could not see it and a colleague's open board kept its old chips. The token now also carries that project's `work_item_label` count and newest `created_at`, which change on every add and every remove — read-side only, because touching `work_item.updated_at` instead would fire every `work_item` trigger for a routine edit. A label RENAME is not covered and cannot happen yet; the settings surface that adds renaming must add `label.updated_at` to the token.

The row cell is the backlog's only read-only property cell, deliberately: labels are a SET, `<InlineEdit>` has no multi kind, and the set's editor is the panel's `L`. Both label actions already revalidate the board and the backlog paths, so a pick in the peek lands on the chips behind it. `labels` joined `PORTAL_FORBIDDEN_COLUMNS` with this slice: the grep matches identifiers, so `labelId` alone would have walked past the very shape a portal task list would reach for.

### 5.4 Side-peek `?item=KEY-123`
Item opens as a right panel over the current view; `⌘↵`/full-screen icon → `/projects/[key]/items/[number]`. Back closes the peek. Panel: title, properties rail, Tiptap description (checklist nodes, paste-upload), subtasks, attachments, comments, Activity tab.

**Activity — shipped 2026-09-14 (panel slice 8).** The section that closes the panel on both surfaces: the item's `WorkItemActivity` history on the one dated rail (§10.15 `Timeline`), newest first, fifty rows a page. A row is WHO (the actor's name; "Unknown" for an id that no longer resolves, "System" for a row that names nobody), WHAT (one of twenty-five sentences built by `item-panel/activity-copy.ts` from the row's field and its four columns — the state a ref names, or its category when the state is gone; a priority label; a duration in the tenant's style; a day in UTC; the two visibility tokens, never a third wording; a member's name; a milestone's, resolved by the read from the row's ref under the reader's own RLS and never stored on it; a label's, resolved the same way — and "changed <property>", the property's own label, for a field whose slice has not arrived or a value the parsers refuse, never nothing; the parsers are strict, so a value they cannot read degrades to that sentence rather than reaching a formatter that throws) and WHEN, the relative time §8 reserves for activity, with the absolute stamp in `title` and `dateTime` (`<RelativeTime>`, the inbox's component too, measured from the server's instant so both renders agree; a stamp ahead of that instant reads "now", never "in 4 seconds"). The node glyph is the field, except for the three fields whose VALUE has a glyph of its own, which draw that: a state change the new category's icon and tone, a priority its bars (No priority excepted — three faint dashes read as a bare dot), a visibility change its icon. **Every row wears its own chip and row cue (§10.4)** — a history row is a class-B row — and the chip is the row's visibility NOW, what the portal would show, not a record of what the client was once shown: making an item private flips its rows INTERNAL in the database and nothing flips them back on a re-share, exactly as the item's own chip forgets, so the two chips always agree. The page is part of `getItemDetail`'s one scoped read, so the section can never show history for an item the panel refused, and the names are resolved in that transaction (every member of the tenant, suspended ones included; a Phase 3 contact actor, bound to the item's own client). Older pages are LINKS on the full page — `?before=<row id>`, the inbox's keyset on the UUIDv7 id, never on `created_at`, through the one `idCursor` parser both surfaces share — and a page past the end is its own state whose verb is "Latest activity"; a cursor that is not one of THIS task's rows (garbage, a stale link, another task's row) is the newest page, never a 404 and never a filter, and the page carries that answer (`before`) so the link and the state cannot disagree with the rows. The peek links out to the full page for older pages rather than paging in place. Not yet: comments are their own slice, and an archive writes no history row (it audits instead, pre-existing).

**Subtasks — shipped 2026-09-14 (panel slice 9).** The section between the description and the attachments, on both surfaces, for every item that can have children — an Epic's section is titled "Tasks", a Task's "Subtasks", and a Subtask has none at all (the lowest level cannot nest; no section rather than an empty one offering a verb the database would refuse). The rows are the item's LIVE children in the project's one order (rank — the backlog's and the board column's), read inside `getItemDetail`'s transaction behind the same scope check as the item (`src/modules/work/subtasks.ts`, kept off the barrel for the reason `readItemActivity` is: a row carries the state NAME, which a contact must never see, and the portal-projections test greps only `portal.ts` files). A row is the state's category icon, the key, the title (struck when done, as the backlog strikes it), the state name, the assignee, "Archived" when it is, and the row's OWN chip and cue (§10.4) — a child carries its own visibility, defaulted from the parent at creation and never inherited live (§3.1), so a private subtask under a shared task wears "Private to team" here as it does everywhere. Soft-deleted children are not rows; archived ones are, so the meter cannot disagree with the list. The meter in the card's header is the plan's progress rule: done over everything not cancelled, "n of m done", and only when m > 0. **Every link stays on the surface it is on**: from a peek over the board a subtask opens as a peek over the board with the member's filters intact (`withItemParam` — the peek's own URL re-addressed, one `?`, no stale error token), from the full page as the child's page; the rail's "Part of" link follows the same rule since this slice. **The add row** is the board column's "+" shape — a button at rest, a title field on click (`h-8`, the rest box, `maxLength` 400 as the row and the column bound it), Enter creates and keeps the field open for the next one, Escape closes the field and hands focus back to the button (and does nothing while a create is in flight: a closed field has nowhere to keep a title the server then refuses), a blur with nothing typed closes — and is a control only for a member who holds `work_item:create` and only while the parent is not archived (a live child under a closed parent is a board card whose parent nobody sees — a UI gate, as the panel's upload gate on the project's status is; `createItem` itself does not refuse an archived parent, a restore-flow question); it is the empty state's one verb ("No subtasks yet · Add subtask"), rendered by the island itself so the island keeps ONE position in the tree across the refresh that lands the first row (rendered as the empty state's `action` it remounted and forgot its field and its announcement), and a reader who cannot create sees a plain sentence, not an `empty` with no verb (§5.8). The copy is keyed by the child LEVEL, one key per level addressed as a dynamic key (`add.TASK` / `add.SUBTASK`, the catalogue's convention for a closed union; its parity test reads ICU `select` branches as arguments, so `select` is not the tool) — an Epic's row says "Add task", "Task title", "Task ACME-9 added" — never a boolean threaded by hand, which reached the button and not the field on its first use. Under a client-visible parent the field carries the attachments' hint ("Follows ACME-12 — this task is visible to the client, so new subtasks start visible too", `aria-describedby`): the child starts visible by §3.1, and a member who holds `work_item:create` but not `work_item:change_visibility` is told so before Enter. The create is `createSubtaskAction` → the one `createItem`, so a subtask lands exactly as any task does — numbered, at the bottom of the project's order, in the default state, visibility defaulted from the parent, a `created` history row and audit of its own — and the tree trigger has the last word (`CANNOT_NEST`, `NOT_FOUND`, each a typed message the row toasts; the title stays in the field). The sent title shows at once as a pending row under the list, keyed by a local id (never by its title, which a sibling may share) and shown only while the transition is pending — `router.refresh()` is issued inside the same transition, so the pending rows vanish on exactly the render that commits the server's row, and a slow refresh never shows it twice. The live region speaks the new key once it exists; the round trip gives focus back to the field only if focus is nowhere, never pulling a member back from a picker they opened meanwhile. **Escape under a layer** now has one contract: the shared `SheetContent` and `DialogContent` wrappers (`src/components/ui/escape-local.ts`) let an Escape through in Radix's own `onEscapeKeyDown` when its target sits inside `[data-escape-local]`, so a field inside any sheet or dialog that owns its Escape marks itself and runs the only handler of that key — the layer's own hook, never a competing listener (the standing trap; `inline-edit.tsx`, the trap's own example, can adopt the marker in its own slice). No key opens the add row (`⌘⇧O` converts a checklist item, a later slice); no reorder within the parent (rank is the project's, the backlog's drag owns it); no delete from the section (the backlog row's menu); no reparenting anywhere (no service reparents — `parentId` is written at creation only).

**Comments — shipped 2026-09-15 (panel slice 10).** The section between the attachments and the Activity section, on both surfaces: the task's LIVE comments oldest first (a thread reads top-down; the newest 200 with a line saying so past that — paging a thread is a later slice), then the §5.6 composer for a member who holds `comment:create`. The rows are `getItemDetail`'s one scoped read (`src/modules/work/comments.ts`, kept off the barrel for the reason `readItemActivity` is: a row carries the author's member id), and each row already carries what the READING member may do to it — `canEdit` (own, or `comment:edit_any`), `canDelete` (own, or `comment:delete`), `canChangeVisibility` (`comment:change_visibility`, and never on a contact's own words) — so the section never decides who may do what. A comment is a class-B row: the author (a member or a Phase 3 contact, bound to the item's own client; "Unknown" for an id that no longer resolves), the relative time §8 reserves for activity, "(edited)" when it was, and its OWN chip and cue (§10.4) — an internal note on a shared task wears "Private to team", and that is the whole point of the two modes. The body is rendered on the SERVER by the static renderer (`src/components/rich-text/render.tsx`): twenty comments cost no editor; the editor (`comment-editor.tsx`, the description's toolbar shared through `editor-toolbar.tsx`) is loaded on demand only when a member writes or edits. The row's verbs live in a `<RowActions>` menu (§5.12): Edit swaps the body for the editor seeded with the words (Update / Cancel, Escape cancels — the editor marks itself `data-escape-local`, so the sheet lets the key through), "Show to client" / "Make private" flips the chip — never optimistically (§10.4) — and Delete asks the §5.9 question in place. Every change runs in a transition with `router.refresh()` inside it, so the editor closes and a deleted row goes on exactly the render that commits the server's answer (the subtask island's rule); a failure is told and the words stay. **The settled rules, from the founder's eight panel decisions (PLAN §0, 2026-09-12), stated once in `comments.ts`:** FLAT in 2W (no `parentId` is ever written; a reply-visibility guard comes first); default = Internal note; "Reply to client" is a mode of the same composer under `comment:create`, offered only on a task the client can see, and refused by the database otherwise (`SUBJECT_NOT_VISIBLE`); one's OWN comment is routine (create and edit write a `WorkItemActivity` row — `field: "comment"`, the verb in `newValue`, the comment's id as the soft pointer — and no audit event), editing SOMEONE ELSE's audits `comment.edited_by_other`, and a delete or a visibility change always audits (`comment.deleted`, `comment.visibility_changed`). The history then says "commented" / "edited a comment" / "deleted a comment" / "changed a comment's visibility to …", each INTERNAL by construction (the field is not portal-safe). The assignee is told (`work_item.commented`, coalesced) unless they wrote it; no mentions yet (the comment schema has no mention node, and `Mention` has no writer). **The database closes the write-skew** the standing trap named (20260915120000): `comment_denorm_guard` reads its subject FOR SHARE and alive for an insert and for any row that is CLIENT_VISIBLE after the write, so a client reply and the task's make-private can never both commit, and a comment posted while its task is being deleted is refused rather than orphaned outside the cascade; a flip to INTERNAL — the safety lever — still never waits on its subject. The service share-locks the task BEFORE it touches a comment row, the order `deleteItem` takes them in, so no cycle closes (`rank-lock.ts` lists the lockers). Not yet: threaded replies, mentions, paste-upload inside a comment, a contact's comment through the portal (Phase 3), paging a thread.

**Checklist → subtask (`⌘⇧O`) — shipped 2026-09-15 (panel slice 13).** The one verb the description and the Subtasks section share: with the caret in a checklist item, `⌘⇧O` creates a child of this task from that line and removes the line. It is offered on exactly the items the Subtasks section is rendered for and under the same cap (`childTypeOf` plus `work_item:create`); where it is not, the key is handed back to the browser. **The order is the design: the child is created FIRST, and the line leaves only once it exists.** A refused create changes nothing and says so; the removal is an ordinary edit in the editor, flushed at once, so a failed save leaves the words on screen and the field dirty — the editor's standing contract. It is written that way so the description keeps exactly ONE writer: a server action that removed the line itself would carry a base token while the editor's own autosave, two seconds behind every keystroke, held the same one, and one of the two would be refused as stale — the member told “someone else saved this description” about their own typing. **The rule is narrow on purpose:** an item that is not a single line — a nested checklist under it, a second paragraph, a line break — is refused rather than flattened, because every way of converting it loses something (a title silently joining two lines, or nested items deleted with their parent); the refusal is actionable, since converting what is nested first makes the parent convertible. **A TICKED item is refused too**, and for the same reason: a new task starts in the project's default state, so converting a finished line would delete the one record that it was finished. Empty and over-long lines are refused with their own words, and the tick is named LAST of the refusals — unticking is the only remedy that is a different action, so naming it first would send the member back for a second refusal. A selection that runs out of the line it starts in names no single line and is refused as "no checklist item"; a selection INSIDE one line still converts the whole line, because a task title is the line. **Under a client-visible parent the child starts visible too** (§3.1) and the answer says so in a TOAST, not only in the sr-only region — a keystroke has no field to hang the Subtasks add row's hint on, and that disclosure must reach the eye. Each outcome speaks on exactly ONE live surface: sonner's region is a live region too, so a sentence written to both would be announced twice — and the sr-only region is CLEARED as each keystroke is handled, or a toast-only outcome would leave the last conversion's sentence sitting in the DOM for a screen reader to browse to. **One conversion per press, and one at a time**: both guards sit before the document is read. OS auto-repeat is refused in SILENCE — a repeat is not a decision anybody made — and the view's own `handleKeyDown` is what records `repeat`, a fact a Tiptap keyboard shortcut is never handed; without it a leant-on key would walk down the checklist minting a row per line, because the delete maps the caret onto the next item. A second PRESS while a create is still in flight is a decision, so it is answered, under a fixed toast id so it cannot stack. The keystroke is ProseMirror's, never the shell registry's — the registry is inert inside an editable element and answers no chord but ⌘K — and the registry carries a `run: null` row purely so the `?` overlay advertises it (§6). If the line is edited during the round trip it is left where it is and the member is told: deleting a sentence they have since rewritten is worse than a line left behind. **Not yet: a pointer affordance.** The verb is keyboard-only, which on a phone means unavailable; a toolbar or row control is owed.

**Timer (`T`) — shipped 2026-09-17 (slice 19).** A start/stop control in the panel's header on both surfaces (in the peek, beside "Open full page") — the 2T goal "per-task start/stop" finally reachable from the task: until this slice a timer started only on `/time` (the quick start, or continue on a week row), and `T` anywhere only stopped a timer or navigated. **Offered** to a member who may track time (`canTrackTime` — permission AND entitlement, asked before any time service runs, the `/home` rule) on a task that is not archived, in a project that is not (the time service refuses an archived project's entries); elsewhere there is no control and no item-scope `T`, so the global `T` keeps its meaning. **Start** starts a timer on this task with the service's defaults — the project's default agreement and its billable default, no work type; choosing either at start is `/time`'s, and a stopped entry is corrected there. If another timer is running it stops in the SAME transaction and the toast offers Undo for 30 s: the quick start's contract, through ONE hook both surfaces call (`time/use-timer-commands.tsx`). **Running here** the control reads "Stop timer" with the elapsed clock beside it, ticking with the pill's own 1 Hz clock. "Is the running timer THIS task's?" is the pill's answer, never a second read: `TimerPillState.running` carries the member's own `workItemId`, the panel's first paint takes the same per-request `getCurrentTimerOnce` the layout's pill does (`loadPanelTimer`), and `useTimerSnapshot` then reads the pill's store — whichever of the store and the panel's own read was taken LATER wins, and a later panel read is published into the store so the pill follows it (a peek opened by a client navigation re-renders the page, not the layout, so its read can be the newer one). A stop in the pill turns the button back into Start (e2e — though that test cannot tell the store from the pill's own refresh, which re-renders the panel too); a stop in another tab does when this one regains focus, through the store alone. **`T` in the `item` scope shadows the global `T`** (the `enabled` mechanism §6 names); it is SWALLOWED while the control's own start or stop is in flight, and a HELD `T` is refused (auto-repeat would flip the timer back — the `X` and `⌘⇧O` rule). "In flight" is the server action plus an awaited re-read of the timer (`syncTimerSnapshot`) and deliberately NOT the page's refresh: a transition around the whole thing stayed pending until the revalidated page had re-rendered — measured at over two seconds on a task page — and every `T` in that window did nothing; once the store holds the server's answer the next press can act on it while the page catches up. (The quick start, which draws the running timer from its own server props, runs every verb — the toast's Undo included — inside a transition of its own (`inTransition`) and stays disabled until its refresh lands.) A second start or stop while one is out is ignored; an **Undo is never ignored** — its toast appears while the start is still settling, sonner removes a toast once its action is clicked, and a dropped undo would take the only way back with it (found by review; e2e clicks Undo with the start's re-read held) — and each toast's Undo acts once, since sonner keeps the button mounted through its exit and a second keyboard press there would send an undo the server can only refuse. The busy button is `aria-disabled`, never `disabled`: disabling the focused button drops focus to `<body>`, where every single key acts behind the peek. **The staff notice comes with the start** (SECURITY.md §9.7.5): the service refuses an unacknowledged start from any surface, and `/time` was the only surface that SHOWED the notice because it was the only one that could start a timer. A member who has not acknowledged the current version gets it here, in a dialog — fetched when they press Start (`staffNoticeAction`, the member's language), never carried on every panel render, and a start that finds it acknowledged meanwhile simply starts. The dialog is a flex column capped at the viewport (`max-h-svh`), so on a phone on its side the text scrolls and the title and buttons stay on screen. Cancel starts nothing and hands focus back to the button (`useFocusReturn`) — and a Cancel, Escape or scrim click made while "I have read this — start the timer" is still in flight leaves the notice acknowledged but starts nothing (e2e, with the acknowledgment held); otherwise the acknowledgment is followed by the start, with no second toast to bury the one that carries Undo. While the dialog is open it owns the keyboard through an `exclusive` `modal` scope that FOLLOWS `open` (its content is not a suppressing layer, so a `p` would otherwise open the Priority picker behind it). **A toast over a modal layer** had never been clicked before this slice, and could not be: Radix's modal layer sets `pointer-events: none` on `<body>`, where sonner mounts its toaster, and treats a pointer-down outside as a dismissal. The `Toaster` wrapper now opts its toasts back into pointer events, `SheetContent` and `DialogContent` keep themselves open for an interaction that starts inside the toaster (`src/components/ui/toast-outside.ts`), and a press on a toast never moves focus — under a modal the trap would hand focus straight back to the field being typed in with its text SELECTED, and the next key would replace it (e2e: Undo over the peek while typing in the subtask row). **Not yet:** Σ spent on a card; Undo by KEYBOARD while a modal layer is open (sonner's Alt+T moves focus to the toaster and the layer's trap takes it straight back). *(`T` on a focused card or row and the running badge shipped in slice 21 — the next paragraph.)*

**Timer on the card and the row — shipped 2026-09-17 (slice 21).** `T` on a board card or backlog row starts a timer on THAT task, or stops it when the member's timer already runs there — the panel's verb, through the panel's code: the decision, the Undo toast and the staff notice on a first start moved out of the panel's control into ONE hook (`time/use-task-timer.tsx`) that the panel runs for its own task and the board and the backlog each run once for whichever card or row the key was pressed in. "In" means focus anywhere inside it: the card itself or its "…" button (asked of the DOM with `closest`, so a portalled menu is in no card), and on the backlog anywhere in the row as for `X` — unlike the board's `S` and roving keys, which need the card itself, because the key `T` would otherwise reach is the global one, which stops a timer or leaves the board. **Under a card's or row's inline delete question** ("Delete this task? Yes No", rendered in place inside it) `T` is SWALLOWED: a timer must not start on the task being deleted, nor the global `T` stop a timer or leave the page behind the question (review round 2 — `closest` had made the card's `T` reachable from its Yes; e2e on both, each mutation-checked). **The notice names the task** when it opens from a card or row ("The timer starts on ACME-12: …"): the dialog covers the list, and the start goes to the task pressed for even if focus has moved on since; Cancel hands focus back to where `T` was pressed (e2e on a card, as the employee). **Offered** where the panel's control is: to a member who may track time, in a project that is not archived, and on the backlog not on an archived row; in each of those the key is left alone and the global `T` keeps its meaning — the panel's rule for an archived task, and it means `T` on an archived row stops a timer running elsewhere (recorded: a later change should claim and explain on all three surfaces together). The pages now always take `loadPanelTimer`, not only for a peek: on a full load or a refresh that adds one `time:track` check (the timer read is the layout pill's own per-request one), but on a CLIENT navigation — a peek, `?group=`, board ↔ backlog — the layout does not render, so it is a timer read of its own too (recorded). **Otherwise the card or row CLAIMS the key** — even while its own start is still in flight (a second `T` is swallowed, never passed on) and on an optimistic card or row with no number yet, where it does nothing: passed down, the global `T` would stop the timer it just started, or the one running elsewhere (e2e: with another timer running, `T` on a row starts the row's task; with none, `T` on a card starts the card's instead of navigating; `T` on a card's "…" button starts the card's; a second `T` while the first start is held sends no second start). The handler applies the dispatcher's guards itself (`focusedKeyApplies`: a held `T` is refused, a `G T` is the go-to). While a verb from a card or row is in flight the board region / the backlog carry `aria-busy` — for assistive technology; there is no visible pending cue, the toast and the badge follow. A card carries `aria-keyshortcuts` for the letters it answers (`J K`, `S` where the member may move it, `T` where a timer can start), a row `J K` plus `T` (its checkbox carries `X`). **The running badge is the member's OWN timer only** — a colleague's clock on a card would be the live presence rule 14 forbids. On the card it is a timer glyph and the ticking clock beside the estimate, with a static accessible name ("Your timer is running on this task") so a reader is not handed a new clock every second; on the row it is the glyph alone, before the title INSIDE the title cell's contained wrapper (§5.3a), where nothing can widen a column — in the key cell, measured to the character, it would have grown the column each time a timer started (review). **Rendering cost is bounded on purpose:** a list reads the timer through `useTimerFacts` (which task, and whether a start needs the notice — plain values that change only on a start or stop), never through `useTimerSnapshot`, which re-renders its reader on the pill's 1 Hz tick; only the one running card's clock (`TimerElapsed`) ticks. `publishRead` gives a tie to the store (`<=`): the layout's read and the page's are one cached read with one `serverNow`, and a card that REMOUNTS after the page loaded (a drag or Move to… puts it in another column) re-publishes the page's copy with its clock — which, re-measured against a server instant minutes old, pulled the pill's clock back. A card's badge needs the page's timer read, so on an archived project's board — where no timer can start — a timer still running from before the archive wears no badge (recorded). A row kept mounted only by the backlog's focus pin stays pinned while focus is in a dialog (`dialog-content`: the stop confirm, the notice, the palette, the `?` overlay), so the dialog's focus return still finds it. **Not yet:** a start in flight is guarded per surface, not across them — `T` on a card and then at once in that card's peek can send two starts (the server serialises them; the first is stopped with an Undo — already true of the pill, the quick start and the panel); the service accepts a timer on an ARCHIVED task (only the project is checked — the UI is the only gate). **Σ spent on a card is a separate slice:** a task's spent time sums every member's entries, which is `time:view_team` data (the project Time tab's by-task table), so it needs its own gated read and dbtest rather than riding on this one.

**Timer on the queue row — shipped 2026-09-17 (slice 24).** The `/home` queue's rows carry the panel's timer verb twice over: `T` on the focused row (the board card's and backlog row's rule, through the same `useTaskTimer` hook — the same decision, Undo toast and staff notice, which names the task), and a **start/stop button** at the row's end for a member who may track time (`canTrackTime`, the page's own `getTimerStateAction()` snapshot — the per-request read the pill and the strip already share, never a second query). The button is the research's "timer one click from Home" (PLAN 2W): a row is a link, a link cannot hold a button, so it stands beside the link in the `<li>`; `icon-sm` ghost, Play or Square, its accessible name saying which task ("Start a timer on {task}" / "Stop the timer on {task}"), never `disabled` while a verb is in flight (a disabled focused button drops focus to `<body>`) but `aria-disabled`, with `aria-busy` on the list. While the member's timer runs on a row the button is Stop in the success tone and the row wears the backlog's glyph before its title (`home-queue-row-timer`, rule 14 — the member's OWN timer only). The rows are a client component now (`home/queue-rows.tsx`) inside the server card; the empty state and the truncation sentence render with no JavaScript as before. `J K` arrived in the same slice (§6 `home`). Not on the queue: a ticking clock (the strip's Running tile ticks 40px above) and Σ spent.

**Σ spent on the card — shipped 2026-09-17 (slice 22).** A board card shows the task's FINISHED time as one figure with its estimate — "⌛ 45m / 2h" — or alone when the task has no estimate; without spent time the estimate renders exactly as before. The read is the board page's own (`projectItemSpent`, one `groupBy` over finished, undeleted entries with a task, scope-checked like the Time tab's rollup), never a ride on `listItems`: a task's spent time sums EVERY member's entries, which is `time:view_team` data, so that permission gates the team figure; a member with `time:track` alone sees their OWN time on the task (rule 14: "employee: own hours"), and a member with neither sees no figure. The glyph is the same in both cases and the accessible name is not: "Spent 45m of 2h estimated" against "Your time 45m of 2h estimated" — the sentence says whose time it is. Over its estimate (the team figure only; own time against a team estimate says nothing) the figure wears the caution tone the column's WIP count wears. Under a minute rounds to nothing and shows nothing, so a card never says "0 min". Running time is not in the figure: the member's own running timer is the badge beside it (above), a colleague's is never shown (rule 14). The figure updates on the next server render — the stop confirm's refresh, a full load, a `?group=` navigation — not on the 12 s freshness poll, which watches the work version that a time entry does not bump (recorded). Not on the backlog row (nothing was asked of it) and not in the column header, whose Σ is the estimate.

**Stop confirm — shipped 2026-09-17 (slice 20).** The 2T plan's "stop → inline confirm (duration editable, note, billable)", reconciled with rule 9's "one tap to stop": **the tap still stops, exactly, at the press**, and only then does a small dialog show what was saved — "Timer stopped", the entry's label and its clock time — with the three adjustments a member makes at that moment: the note, the duration (typed in the one grammar, `1h 30m` · `90m` · `1,5`, prefilled as whole minutes) and billable (a checkbox on a project entry; an instant task says it is not billable and offers no choice). A confirm that kept the timer running until it was answered would have billed the seconds spent typing the note, which is why the order is stop-then-confirm. It is a dialog, not §5.9's in-place confirm: the stop can come from a key or the header pill, where there is no row to confirm in. **Done** sends only what changed, through the ordinary entry edit (`updateEntryAction` → `updateEntry`, audited `time_entry.updated`): nothing changed is nothing sent, and an untouched duration keeps the entry's raw seconds. **A new duration keeps the END — the instant Stop was pressed — and moves the start** (`durationAnchor: "end"`): at that moment the end is the one timestamp known to be true, and a longer duration ("I started earlier") can never put it in the future. The service now refuses any edit that changes a positioned row's interval so that it ENDS in the future (`createEntry`'s rule, with its minute of slack, now with its own message, `ENDS_IN_FUTURE` — the week grid's inline duration, which keeps the start, meets it), re-reads the row under the member's lock so two edits of one row cannot write each other back, and judges length and overlap — and clears an auto-stop's "needs review" — only when the interval itself changed — so a note or billable edit on a row that overlaps from before a tenant switched blocking on, or one that became longer than 24 h through a long auto-stop, is no longer refused for a field the member did not touch (dbtests, mutation-checked). **Escape or the close button keep the entry exactly as stopped** — typed changes are not sent (e2e counts the requests after Escape and re-reads the week; the close button is exercised too) — **except while a save is in flight**, when the dialog does not close (e2e, with the save held) and its fields are read-only, so what is on screen is what is being saved. A refusal (a duration the parser cannot read, an instant task's emptied note, a start moved into an overlap a strict tenant blocks) is a toast and the dialog stays open with what was typed; the form does not submit again once the confirm has closed, through its exit animation. A save that never answers keeps the dialog open until the page is reloaded — the accepted price of never pretending a save was discarded. **Focus lands on Done, never in a field** (e2e: typed `t`s neither reach the note nor the global `T`): the dialog opens when the stop has ANSWERED, a beat after the press, and a text field focused with its text selected — Radix's default for the first tabbable — would take the member's next keystrokes (a second `T`, a held one, the next task's name still being typed) and an Enter would save them into the entry. Shift+Tab steps back to the fields. A held `T` is refused by the pill's global binding too. **Every explicit stop opens it** — the pill's Stop, the global `T` (e2e), the quick start's Stop, a task's control and its `T` — and it replaces the old "Stopped after …" toast; a stop that is a side effect — starting another timer (that is the Undo toast's), a break (e2e), clocking out — does not. **One host** (`components/shell/stop-confirm.tsx`), mounted once by the shell for a member who can track time and opened through `openStopConfirm`, so it behaves the same on any page and over the item peek (e2e: stopped inside the peek, typed, Done — the peek stays open and focus comes back into it). Its open and saving flags live in the module beside the entry, so a remounted host never re-opens an answered confirm and a save that lands after a NEWER stop's confirm opened touches only its own. It owns the keyboard through an `exclusive` `modal` scope that follows `open`, and returns focus where the stop was pressed when that element still exists (`useFocusReturn` — a task's control, e2e); the pill's and the quick start's Stop buttons are disabled while their stop is out, so focus has already fallen to the page and nothing is returned. Not yet: a per-member "don't ask after a stop" preference; editing an entry's agreement or work type after it was created — no surface does, and the confirm is also the only place a note can be changed after a stop; `updateEntry` re-resolves the rate snapshot on every edit, so a note-only fix made after a rate-card change reprices the entry (pre-existing, recorded).

### 5.5 Visibility badge — two tokens only
`Private to team` / `Client can see`. Every class-B row shows one; never a third wording, never an icon alone. Inheritance tooltip: "Follows ACME-12". Bulk change shows the count: "Make 14 tasks private?".

**The rendering is specified in §10.4, which supersedes the "neutral / accent" wording once used here** — the pair now differs on five channels (fill, icon, shape, weight, border) plus a 2px row cue, and is measured under three colour-vision simulations by the release gate.

### 5.6 Comment composer — two modes
Toggle "Internal note" (default, neutral) / "Reply to client" (distinct colour, border). Mode maps 1:1 to visibility; contact-authored comments have no toggle. Mentioning a Contact in an internal note warns inline. `⌘Enter` posts.

**Shipped 2026-09-15 (panel slice 10, `item-panel/comment-composer.tsx`).** The toggle is a `radiogroup` whose client option wears the read chip's warm fill and pill when chosen (§10.4: the write control is never less legible than the read one) and the internal option the chip's outline; it is offered ONLY on a task the client can see. On a private task there is no toggle at all — the badge says "Private to team" and the hint "Follows ACME-12", exactly as the upload form does — because a client reply there is what the database refuses. A hint under the field says who will see the words BEFORE they are posted ("Only your team sees this note" / "The client will see this reply on their portal"), and the verb itself names the mode ("Post note" / "Post reply to client"). `⌘Enter` (`Ctrl+Enter` on Windows and Linux) posts from inside the text; the button is disabled while the comment is empty (the server refuses `COMMENT_EMPTY` anyway). The mode is kept between posts; the composer is keyed by the item AND its visibility, so a task made private under an open composer remounts it rather than posting a pick that is no longer offerable. The mention warning waits for mentions (not in 2W).

### 5.7 Step-up dialog
One `<StepUpDialog>` reused for every ✦ action (role edit, cost reveal, credential reveal/share/export/visibility, member removal…). TOTP field, "why" line, `vault.stepUpMinutes` explained. Never a custom MFA prompt elsewhere.

### 5.8 Empty state
One verb + one sentence + one primary action, e.g. "**Create** the first task — press C or click +". No illustrations larger than the text. Onboarding checklists are skippable and dismissible for good.

`variant="empty"` **requires** an `icon` and an `action` — enforced by the type, not by review. An open folder on a Team tab and an open folder on a Board tab say nothing about either, and a "nothing here yet" with no verb is a dead end. A state that genuinely has no verb to offer is not `empty`: it is `filtered` (clear the filter) or `forbidden` (this is real, it is not yours). The audit asserts the runtime half — every `[data-variant=empty]` contains a focusable descendant.

### 5.9 Inline confirm
Destructive actions (delete, archive-with-children, lock, revoke) confirm **in place**: the trailing slot becomes `Delete "file.pdf"? [Yes] [No]`; no modal. Undo toast where reversible. Esc and focus leaving both cancel; focus moves to `Yes` when the question opens and returns to the trigger when it closes.

**Resting weight and confirm weight are separate.** The resting control is `variant="outline"` (or a `tone="danger"` menu item, §5.12); the `Yes` of the question is `variant="destructive"`, and that is **the only solid `--destructive` fill in the product's resting UI**. A page-level destructive — Archive client, Archive project, Delete role — is not a button floating on the canvas either: it gets its own `<SectionCard title={t("danger.title")}>` at the foot of the main column, one line of consequence, and the outline resting weight.

### 5.10 No Save buttons
Forms outside `/settings/*` and auth do not have Save; settings forms auto-save per field with a saved-tick and undo. Rule 3 tripwire. *(Recorded exception, 2026-09-17: the stop confirm's **Done** (§5.4) is a confirm, not a Save — the entry is already saved when the dialog opens, Escape keeps it, and Done applies the member's adjustments to a record that exists.)*

### 5.11 `<InlineEdit>` — a value is text until you edit it (FOUNDER MANDATE 1)

**A page of labelled properties is content, not a form.** A record property renders as TEXT and becomes its control on activation. An always-mounted `<Input>` for a value nobody is editing turns a Timeline into a data-entry screen, makes the milestone *name* the lowest-contrast thing in its own row (32px of chrome around 13px of text), and advertises the browser's `yyyy-mm-dd` on every undated field.

- **Rest** is a real `<button data-slot="inline-edit">`: transparent background, `1px solid transparent` border, and the **same box** as the control it becomes — `px-2.5`, `h-8` (`h-7` at `density="table"`). `hover:bg-accent hover:border-input`. Focus is the standard `outline-2 outline-offset-2 outline-ring`, never a ring shadow (§9). A `select` shows its chevron on hover/focus only.
- **Empty** shows a `placeholder` verb in `--muted-foreground` — "Set date", never a native date placeholder.
- **Editing** mounts the existing `Input` / `Input type="date"` / `NativeSelect` / `Textarea`, uncontrolled, autofocused, text selected. `key={value}` re-seeds it from a server refresh.
- **Commit is unchanged**: blur for text and multiline, `change` for select and date — the two events `<AutoForm>` already listens for. `InlineEdit` adds **no second save path**.
- **Keyboard**: Enter / Space / **F2** open; Escape restores the value into the control *before* blurring (so AutoForm's focus snapshot matches and **nothing is posted**) and returns focus to the trigger; Tab out commits; `⌘/Ctrl+Enter` commits a multiline.
- **ARIA**: the trigger's accessible name is `common.inlineEdit.trigger` — "Edit due date, currently 12 September 2026" — the mode change is announced in a live region, and the trigger is the **only** tab stop for the value.
- **Failure is not lossy.** `invalid` keeps the control open with `aria-invalid` and the user's text intact. Reverting to the server value while the edit is still on screen is a bug this product shipped once.

**The hazard, stated once so nobody re-introduces it.** `<AutoForm>` posts the *whole* FormData and several server actions read an absent field as an erase. So `InlineEdit` **always renders `<input type="hidden" name value>` at rest**, and never mounts it at the same time as the real control (`FormData.get()` returns the first entry, and a stale hidden value would win). Every action reached this way is additionally `has(formData, name)`-guarded. `e2e/inline-edit.spec.ts` is the regression gate: it edits a milestone's name alone and asserts, in the database, that the due date still names the same day and the visibility is unchanged.

`VisibilityInlineEdit` is the safety-critical companion: its rest state **is** the `<VisibilityBadge>`, so it carries all five §10.4 channels, which a 28px filled `<select>` drops two of.

**Not for**: a preferences page (a control panel *is* its switches, §5.10) or a writing surface (`ClientNotesForm`, `internalNotes`, `scopeSummary` keep their textarea). Inline edit is for *labelled properties*.

`<PropertyPicker>` (§5.2) is Mandate 1's sibling for a **work-item** property: its rest state is likewise the value as text and the popover is the editor, so a `<button>` trigger inside a `<dl>` or a table row is the sanctioned control there. The shared resting box lives in `src/lib/control-classes.ts` — a **directive-free** module, because a `"use client"` module's exported constant interpolated into a server component's `className` becomes a throwing client reference.

### 5.12 `<RowActions>` — the row's verbs live in a menu (FOUNDER MANDATE 2)

A solid `--destructive` button repeated on every row of every table is the highest-chroma object on most pages and outranks the row it serves.

- The trailing cell holds **at most one** ghost `size="icon-sm"` icon button for the row's everyday verb (download, open), then a ghost `⋯` trigger. Nothing else.
- The menu is the existing `ui/dropdown-menu`. Destructive items are `variant="destructive"` — danger **text**, tinted hover — and **no `variant="destructive"` `<Button>` may exist inside a table row anywhere in the app**.
- `tone: "danger"` **requires** `confirm`; the type refuses it otherwise. Selecting it does not act: it closes the menu and swaps the trailing slot for the §5.9 question in place.
- Reorder arrows, "Move up" / "Move down" and every other row verb go **into the menu**, not behind hover: a hover-only control is unreachable by touch and invisible to a keyboard user scanning the row.
- A menu whose items are all disabled is not focusable, so a reason nobody can read is worse than no control: render **nothing** in that row instead, and let the row's own marker ("(you)") say why.
- Icons are client-only — a lucide component does not serialise across the RSC boundary, so a server component passes items without `icon`.

### 5.13 `<FileDropField>` — the OS input never shows (FOUNDER MANDATE 3)

The dashed region is a `<label htmlFor>`. Inside it: a 16px `UploadIcon`, the hint, and a **`<span>` wearing `buttonVariants({variant:"outline",size:"sm"})`** — not a `<button>`, which would swallow the label's click. The real `<input type="file">` is `className="sr-only"` — **never** `hidden`, never `display:none`, both of which take it out of the tab order and the AT tree. The label shows the input's focus ring via `has-[input:focus-visible]:outline-*`. Dragging swaps to a solid border and a "Drop now" hint; a chosen file replaces the hint with `filename · 1,2 MB` plus a ghost `×`. Disabled is `--bg-disabled`, never opacity.

### 5.14 `<Disclosure>` — one progressive-disclosure trigger

Fields that are empty, and matrices nobody has asked for yet, wait behind one 28px ghost trigger with a rotating chevron: `<details>`/`<summary>`, so it is keyboard-operable, announced as expandable, and findable by find-in-page — which a React-state disclosure is not. Hidden inputs inside it still post, so a closed disclosure and an open one send identical FormData.

### 5.15 `<PageState>` — one component for every whole-page state

404 (anonymous and in-app), the error boundary and an unusable invitation are four renderings of one idea, and they used to differ in every dimension. All four are now `<Page>` → `<SectionCard>` → `<EmptyState titleAs="h1">`: a 22px h1 (the page-title role, §10.7), inside a card, at the content column's normal top-left — no `min-h`/`justify-center` gymnastics — and **always at least one action**, because a whole-page state that offers nothing to do is a dead end.

---

## 6. Keyboard map (scopes `global`, `board`, `backlog`, `home`, `inbox`, `triage`, `item`, `modal`; shown in `?` and the ⌘K palette)

*Registry — **SHIPPED 2026-09-12** (panel slice 5), superseding the 2026-08-31 note that named `react-hotkeys-hook`. It is **hand-rolled**: `src/lib/keymap.ts` holds the pure decision (`decide()`), `src/components/shell/use-hotkeys.ts` holds the scope store and the ONE `window` keydown listener, and a surface registers keys with `useScopeKeys(scope, bindings)`. `react-hotkeys-hook` is **not** a dependency and will not become one — ARC-24 records the four specific reasons. The `?` overlay and the palette's "On this page" group are now PROJECTIONS of the live registry, so board follow-up (ι) is closed.*

*The scope set is the union of this section's original list and ARC-24's, plus `modal`. Precedence is a **table** (`SCOPE_ORDER`: global 0 · board/inbox/triage 10 · item 20 · modal 100), never mount order — React runs child effects before parents', so a push-ordered stack would put the shell's `global` above the panel's `item` and invert shadowing. `board` ships region keys today; `inbox` and `triage` are declared and empty, so their slices are a registration rather than a redesign; `modal` is the honest name for a layer that owns the keyboard while it is open.*

*Dispatch order, once, so nobody re-derives it: `defaultPrevented` → ⌘K → any other modifier → an editable target → a menu/listbox/popover/command layer → **an armed `G` consuming exactly the next key in every scope** → scopes highest-order first → `g` arms. That last-but-one step is how `G S`, `G C` and `G T` coexist with bare `S`, `C` and `T`: they are two events in time, not two meanings of one key. A binding with `run: null` is documentation-only (advertised, handled by a React-tree handler that needs the focused element) — and since 2026-09-17 it **claims no key in the overlay or the palette**: wherever no such element holds focus the dispatcher skips it and a lower scope's binding acts, so that binding's row is listed beneath it (the board's focused-card `T` above the global `T`); a higher scope's claim still hides it. A binding WITH a `run` that is declared but `enabled: false` **swallows** its key rather than letting a lower scope have it; a disabled `run: null` binding only drops its row.*

| Scope | Key | Action |
|---|---|---|
| global | `C` | New task (title-only) in context — **shipped 2026-09-18** (slice 29), rule 2. A dialog in the shell, mounted on every authed page, because "anywhere" is the key's whole point. The project comes from the ROUTE (`/projects/{KEY}/…`) when there is one and from a picker otherwise, and stays changeable either way — it is the only property this surface has, and a member who pressed `C` on the wrong page should not have to close and navigate. The project list is read ON OPEN (`quickCreateProjectsAction`, the scoped `project:view` list the /projects page draws), never rendered into every page of the product. Three commits, rule 2 verbatim: **Enter** creates and leaves the field empty and focused for the next, keeping the project; **⌘⇧Enter** is an alias of it here, since the project is the only property there is to carry; **⌘Enter** creates and opens the task's own page. Offered only with `work_item:create`, read off the SAME batched permission query the nav uses; scope and `createItem` still decide what may actually be written. The `board` scope's `C` below shadows it by scope order — and the shell ALSO stands its own binding DOWN on a board route, declared-but-disabled so the key is swallowed rather than falling through: scope order only decides once both bindings are registered, and the shell is interactive before a streamed page is, so a `C` pressed into that window opened the dialog over a board that was about to claim the key |
| global | `⌘K` | Palette |
| global | `?` | Keymap overlay |
| global | `G H` · `G P` · `G B` · `G L` · `G T` · `G I` · `G V` | Go to Home · Projects · Board (current project) · Backlog · Time · Inbox · Vault |
| global | `T` | Stop the running timer, else go to `/time`'s quick start. While a task's panel shows its timer control the `item` scope's `T` (below) shadows it — not on an archived task or project, nor for a member who cannot track time; a board card or backlog row that holds focus claims it too — by `preventDefault` in its own handler, not by registry shadowing (`board` / `backlog` `T`, below) — and anywhere else on those pages it is this one |
| board | `C` | New task in the default column — inert while the item peek is open |
| board | `S` | "Move to…" (state × position, §7.1) on the focused card |
| board | `T` | Start a timer on the task of the card focus is in (the card or its "…" button), or stop it when the member's timer runs there — **shipped 2026-09-17** (slice 21), §5.4 "Timer on the card and the row". Registered `run: null` and handled on the board region; not offered for a member who cannot track time or on an archived project's board (the global `T` acts there), nor while the peek is open (focus is in the sheet); under the card's inline delete question it is swallowed, for every member |
| board | `↑ ↓ ← →` / `J K` | Move focus between cards and columns |
| backlog | `↑ ↓` / `J K` | Move focus between the task rows — **shipped 2026-09-16** (slice 15). Every item row is focusable (`tabIndex=-1`, by key and never by Tab: a `0` would add a stop before each row's own checkbox on a list already four stops a row, and buy nothing, since a member enters the flow from any control in a row). `J K` and `↑ ↓` act from anywhere in the row (`rovingStep` in `keymap.ts` is the ONE table of which keys are a one-row move, for every list to come); the arrows are left to a control that owns them (`ownsArrows`: a closed select or combobox trigger, a spin button, a slider, a radio) and to Shift, which a list keeps for range selection. Item rows only: a group header is stepped over, and the ends are the ends — the key does nothing there, and an arrow is left to the page. Auto-repeat is allowed by a declared policy (`focusedKeyGuards(…, { repeat: "allow" })`: a move is one row per event, so a held `J` walks the list, the board's rule), where `X` refuses it. Handled on the table body, as `X` is, and refused there while the peek is open; needs no permission — a viewer walks the list too. **`J` alone also carries a registry `run`, and it fires only when NO row holds focus** (a row's own `J` is handled and prevented before the dispatcher looks): it is the ENTRY into the list — from the page, the filter bar, the table's scroll region, or after a focused row was scrolled past the pin and unmounted with focus on it — landing on the first row on screen. Not a palette row. A `tabIndex=-1` row is also focused by a CLICK on its whitespace (intended: `X` and the arrows then act there, and the arrows walk rows instead of scrolling the page); the row carries `aria-keyshortcuts="J K"`. Under the virtualised window the handler only NAMES the row (`focusedRowId`); the window's overscan has the next row mounted already, the focus pin is the belt if a scroll event were ever dropped, and an effect with NO dependency list focuses the named row once it is in the DOM — no dependency list because the row may arrive on a later render than the one that named it, and a second `J` from the same row names the same id, which is no state change (an effect keyed on the id never retried, and the keys went dead — found by review). The effect tells three cases apart by where focus is: inside the named row already (a click or Tab into a control) — nothing; elsewhere in the table — a step, `focus()` scrolls the row into view; on `<body>` — the row was unmounted with focus on it and has come back, so focus is restored WITHOUT scrolling. A step whose target lies outside the pin's reach (`FOCUS_MARGIN`) is refused rather than named: naming it would unmount the current row too and drop focus to `<body>`, which Chromium reports with no blur. `focus()`'s own scroll pulls the window along; the rows carry `scroll-mt-16` / `scroll-mb-16` so a focused row clears the sticky header and the sticky bulk bar. **The focus ring is `TableRow`'s standard outline at `-outline-offset-6`, keyed on `:focus`** (§9 has the geometry: 2px cue border, 2px selected bar, then the ring — a -2 offset painted the ring over the client-visible cue and -4 over the selected bar, both found by review; `:focus` rather than `:focus-visible` because programmatic focus inherits the previous element's focus-visible state in Firefox and WebKit). Not yet: Enter to open the focused row's peek, `Home`/`End`, and the table is a plain `<table>` rather than an ARIA grid, so a screen reader announces a focused row by its content only |
| backlog | `X` | Select or deselect the focused row — the row's checkbox by keyboard, the bulk bar's cap included, and only where that checkbox is DISPLAYED: below the `medium` rung of column priority (a 38rem table box, §10.12) the select column drops — on a phone, and since 2026-09-16 also on a desktop from 768 px to ~880 px with the rail open, where the box is 496–607 px (a classic page scrollbar narrows the box, so the band reaches ~897 px there; an iPad in portrait with the rail open has no bulk selection until the member collapses the rail; before, the column rendered there inside a table 602 px wider than its box) — and a row has no selected cue of its own (`TableRow`'s selected style is an inset left bar in `--primary`, at the edge where `visibilityRowCue` marks a client-visible row, in a colour that measures 1.0002:1 against that mark), so `X` there would select rows nobody can see marked — **shipped 2026-09-15** (panel slice 14). "Focused" means DOM focus inside the row (its checkbox, key link, a cell's resting control, its menu trigger, or since slice 15 the row itself) — except the row's inline delete question, which renders in the row and under which a selection must not change; with `J K` (the row above, 2026-09-16) this is the one-hand flow: `J`, `X`, `J`, `X`. The row checkbox carries `aria-keyshortcuts="X"`, and a member who empties the selection hears "0 tasks selected" (an emptied live region is otherwise silent; a filter that hides every selected row stays silent, as it always was). `X` needs the checkbox DISPLAYED, not in view: a table whose content outgrows its box can scroll sideways past it and `X` still acts — recorded, not refused, because a key that worked or not by scroll position would be worse, and the bar's count is on screen. Registered `run: null` in the `backlog` scope and handled on the table body, for the board's `S` reason: only the event target knows which row, and a window binding reading `document.activeElement` would claim `X` page-wide. The handler runs before the dispatcher, so it applies the dispatcher's guards itself (`focusedKeyApplies`, pinned to `decide()` by a unit test over every combination) plus one more: auto-repeat is refused, because a held toggle flips on every repeat. No ⌘K row: the palette runs only run-bearing rows, and opening it moves focus off the row. Hidden from the overlay for a member without `work_item:edit`, while the peek is open, and wherever the select column is not displayed (observed on its header cell — a media query cannot see the rail). **Not in the item panel**, deliberately: the bar belongs to the list behind the peek's scrim, the board has no selection, and the full item page has no list — a panel `X` would change a selection nobody can see |
| backlog | `T` | Start a timer on the focused row's task, or stop it when the member's timer runs there — **shipped 2026-09-17** (slice 21), §5.4 "Timer on the card and the row". "Focused" as for `X` (focus anywhere in the row); under the row's inline delete question it is swallowed and does nothing, for every member; registered `run: null` and handled on the table body. Otherwise not on an archived row, not for a member who cannot track time, not on an archived project — in each of those the key is left to the global `T` — and not while the peek is open, where focus is in the sheet (and the item's `T`, or on an archived task the global one, answers) |
| home | `↑ ↓` / `J K` | Move focus between the queue's rows — **shipped 2026-09-17** (slice 24), §5.4 "Timer on the queue row". The focus target is the row's LINK, not a `tabIndex=-1` row as on the backlog: a queue row is one link and (for a member who tracks time) one button, so landing on the link means Enter opens the task from wherever a `J` left the member and Tab reaches the button — a focusable `<li>` would have added a stop Enter can do nothing on. `rovingStep`'s one table, `ownsArrows` and Shift left alone, a held `J` walks; the ends are the ends (a letter there is consumed so it cannot reach the registry's entry `J`, an arrow is left to the page). The registry's `J` is the ENTRY from anywhere no row holds focus, landing on the first row clear of the sticky header |
| home | `T` | Start a timer on the focused row's task, or stop it when the member's timer runs there — **shipped 2026-09-17** (slice 24). The backlog row's rule: "focused" is focus anywhere in the row (the link or its button), `run: null` and handled on the list, claimed (`preventDefault`) even while a start is in flight, and left to the global `T` only where this member can start no timer (`canTrackTime` false — the queue never lists an archived task or an archived project's). The row also carries the verb as a BUTTON, the "one click from Home" of rule 8 |
| global | `N` | New time entry (2T; on `/time` and item) |
| global | `Esc` | Close peek/picker/palette |
| item | `S` · `A` · `P` · `E` · `D` · `V` | State · Assignee · Priority · Estimate · Due date · Visibility pickers (§5.2) — **shipped** (`S` in slice 5, 2026-09-12; `P E D` in slice 6 and `A V` in slice 7, both 2026-09-13; `M` and `L` in slices 11 and 12, 2026-09-15); `X` is not an item-panel key — it shipped on the backlog (below). `G A` (account) coexists with the rail's bare `A` exactly as `G P` and `G S` do — an armed `G` consumes the next key first |
| item | `⌘⇧O` | Convert focused checklist item → subtask — **shipped 2026-09-15** (slice 13). The key is the EDITOR's (a ProseMirror keymap), never the registry's: the registry is inert inside an editable element and answers no chord but ⌘K. The registry carries a `run: null` row so the `?` overlay still advertises it |
| item | `T` | Start a timer on this task, or stop it when the running timer is THIS task's — **shipped 2026-09-17** (slice 19), §5.4 "Timer". Swallowed, not passed to the global `T`, while its own start or stop is in flight; offered only where the panel shows the control |
| item | `⌘Enter` | Create-and-open (in create field) |
| item | `⌘⇧Enter` | Create-another with same properties |
| item | `⌘Enter` (composer) | Post comment |
| item | `↑ ↓` / `J K` | Move focus in list/column; `← →` across columns |
| inbox | `J K` · `E` · `U` · `S` | Next/prev · archive · mark unread · snooze — still unshipped, but the **stated blocker is gone**: since 2026-09-12 a surface ships a key by adding one `useScopeKeys` call and gets the overlay row AND the ⌘K entry for free. What remains is the product question of what each verb should do on that surface. (The selection bar's `X`, once in the same position, shipped on 2026-09-15 — the `backlog` row above.) |
| triage | `A` · `D` · `U` · `S` | Accept · Decline · Duplicate-of… · Snooze |

Rules: single keys are inert while an input has focus **or while focus is inside a menu, listbox, select, popover or command list**; `⌘` = `Ctrl` on Windows/Linux and the overlay renders the right glyph; no key is bound that a browser or screen reader needs (`⌘L`, `⌘F`, `Tab`). A non-list body in a popover — the due-date calendar — is inert to single keys through `[data-slot="popover-content"]`; it never renders in a Sheet or Dialog (those are deliberately unsuppressed), and it calls `preventDefault` only on the keys it consumes and never `stopPropagation`, so ⌘K still reaches the dispatcher from a focused day (§9). ⌘K from inside such a layer opens the palette WITHOUT its "On this page" group (`paletteOffersPageRows`, decided once at open; a ⌘K that lands in a dialog still playing its exit animation is judged by where that dialog returns focus, not treated as no layer), so a palette row cannot do what the key could not — stack a second picker on the first. Every dialog nothing opens from a trigger — the palette, MovePicker, the `?` overlay and the shell's More sheet — returns focus on close to where it was opened (`useFocusReturn`): Radix returns it to a trigger that does not exist, which left it on `<body>` with every single key live behind the layer still open. A dialog opened from inside one that is CLOSING inherits that one's origin (the palette's "Keyboard shortcuts" row hands over to the overlay in one batch); one opened over a dialog that stays open returns focus into it. A return made by `useFocusReturn` never opens a tooltip: a Radix `TooltipTrigger` opens on any focus a pointer did not cause, so the shared `TooltipTrigger` wrapper (`src/components/ui/tooltip.tsx`) refuses that one focus event for every trigger in the app (`isReturningFocus`). Radix's own focus returns — a popover or menu handing focus back to its trigger — are not flagged; no tooltip wraps such a trigger today. **Recorded exception:** the item peek's Sheet does not use the hook — its close is a navigation, and focus is still left on `<body>` while it happens.

**`Esc` is never a registry binding, in any phase.** It belongs to Radix's dismissable layers, which handle it on `ownerDocument` at *capture* while `defaultPrevented` is still false — a hand-written bubble handler cannot beat one, and a capture-phase registry handler would break the pinned peek-close contract in `e2e/work.spec.ts`. A component that needs to own Escape wants a Radix layer or an `exclusive` scope, not a key binding. *(An `exclusive` scope stops the dispatcher's walk whether or not it binds anything, so a component that registers one conditionally must gate `exclusive` on being open — `modal` is the top of `SCOPE_ORDER`, so a permanently-exclusive empty scope silently kills every key in the app.)*

---

## 7. Interaction rules

### 7.1 Drag and drop (ARC-17)
- Desktop only, `@atlaskit/pragmatic-drag-and-drop`. Drop indicator line between cards, edge auto-scroll, dragged card ghost at reduced opacity, column highlights on hover.
- Board drop = state change + rank change in one Server Action; backlog drop = rank change *(shipped 2W-F slice 2: the anchor is the row the pointer was over and the side is which half — resolved for display into one convention, "the row it lands above", so both halves of a seam paint the same pixel)*; group-by view drop = property change + rank *(still deferred — a rank-only move carries no property field, so a drag that would leave its group is refused on both surfaces)*.
- A gated state (`requiresApproval` — the seeded Done, 2W-R) is not a drop/create/picker target for a non-approver, the same rule as TRIAGE (`canEnterState` in `src/lib/work-view`, one rule for every surface); `transitionState` refuses server-side regardless.
- Keyboard/mobile twin: "Move to…" (`⌘K` action, item menu, mobile long-press) → picker of state/position ("Top of To do", "After ACME-9", "Bottom"). *On the BACKLOG the twin is the row menu's Move up / Move down / Move to top / Move to bottom (§5.12: reorder verbs live in the menu), because a rank-only move has no state to pick. A verb whose anchor does not exist — the first row's "Move up", a group's last row's "Move down" — is left out rather than rendered inert, and the anchors are the VISIBLE neighbours, so a filter never makes a nudge swap the task with a row nobody can see.*
- Rank is server-computed (fractional-indexing, neighbours locked). The client sends `{ itemId, stateId?, beforeId?, afterId? }`, never a rank string; rank never appears in the DOM.
- Multi-select drag moves the selection preserving relative order.

### 7.2 Optimistic updates (ARC-18)
- Pattern: `useOptimistic` for the affected slice → Server Action → `router.refresh()`; on error, revert and show a toast with the server reason (i18n key) and Undo/Retry when applicable.
- Every mutation returns the canonical row(s) so the optimistic state is replaced, not merged. *(Clarified 2026-09-12: this governs the **server action's** result, not internal transaction steps. `transitionState` now returns the row its `UPDATE` was already fetching and discarding — a `select` was added at the same time, so a 512 KB ProseMirror description stops being read back on every board drop, bulk change and import — and the state action carries it out with an explicit `changed` flag. Where `revalidatePath` cannot reach a dynamic segment, `router.refresh()` is the replacement mechanism: re-rendering the server component IS the replacement.)* *(Extended 2026-09-13, slice 6: S, P, E and D are four per-property `ActionResult` actions in `backlog/actions.ts`, shared by the backlog table's cells and the panel's pickers — `setPanelStateAction` was folded into `setItemStateAction`. `updateItemFields` returns what the column STORED through an inline `select`, and a patch that changes nothing writes nothing and says `changed: false` — compared under the item's row lock (`FOR NO KEY UPDATE`, `lockItemRow` in `rank-lock.ts` — `changeState`, behind `S`, takes it too), so it holds under concurrent edits and each activity row's old value is the one the UPDATE replaced; board and backlog revalidate only when `changed`; and the panel's `usePanelCommit` adopts the canonical row and refreshes on both `changed` branches and on failure, ignoring the answer of a commit a newer pick has superseded.)* *(Extended again 2026-09-13, slice 7: `A` and `V` are two more per-property `ActionResult` actions — `setItemAssigneeAction` and `setItemVisibilityAction`, the last two `FormResult` property actions gone with them — and `assignItem` and `changeItemVisibility` return the canonical row with `changed`, diffing under the item's row lock; `setItemArchived` locks too, and the multi-row writers — `moveItem`'s state leg and the bulk verbs — lock the rows they diff under the project's queue lock, so no writer that diffs a work item against its own read reads it unlocked any more (`updateItemDescription` keeps its own compare-and-set on the document, and `deleteItem` diffs nothing; `src/modules/work/rows.ts` makes the locked, scoped read the default and an unlocked one an explicit opt-out). The one exception to "the pick is shown before the server answers" is `V`, never optimistic (§10.4).)* **Deferred, with its reason:** §7.2's stale-conflict revert ("Updated by <chip> — refreshed") is not implemented for the item panel — it has no freshness poll and `transitionState` has no compare-and-set token, and inventing a second concurrency mechanism beside `updateItemDescription`'s would be worse than naming the gap.
- Freshness: version poll every 12 s while visible + refresh on window focus; no WebSockets/sync engine in v1. A stale-conflict (row `updatedAt` newer than the client saw) reverts with "Updated by <chip> — refreshed".
- Never block the UI on audit/notify side effects — they are inside the same tx server-side (TENANCY recipe), invisible to the client.

### 7.3 Navigation
- Side-peek is default; full page only by explicit action or direct link. Browser back closes the peek. Filters live in the URL, so back/forward restores views.
- 404 (not 403) for anything out of scope, portal or member — the page must not reveal existence.

---

## 8. i18n and formatting

- Languages: **sv + en** from Phase 1b (`next-intl`); locale resolution member → `Tenant.defaultLocale` → Accept-Language → en. Portal locale = contact preference → tenant default.
- **No literal user-facing strings in JSX** (`src/app`, `src/components`) — ESLint rule; keys `<area>.<screen>.<element>`.
- **State categories are translated** (`Planned / In progress / Done` in the portal; the six category labels internally); **state names are tenant data** and rendered verbatim, never translated — *amended 2026-09-01 for UNTOUCHED SEED NAMES ONLY (DATA_MODEL §6.14): a state still wearing its seeded default stores no name at all and renders through i18n in the VIEWER's language, so an English account no longer reads a Swedish tenant's board in Swedish. The moment a tenant renames a state it becomes tenant data and this rule applies to it verbatim, forever. **Scope:** this is the behaviour of projects seeded AFTER 2026-09-01. Projects that already existed carry stored names and keep rendering them to every viewer; they gained the `seedKey` identity in the same migration, so opting one in is a single statement, but that discards names tenants typed and is a founder decision, not a deploy.* Label names, project names and milestone names are unchanged: always tenant data, never translated.
- Dates: `src/lib/week.ts` + `src/lib/calendar.ts` + `Intl` — the memoised `dateFormat` in `src/lib/format.ts`, with `timeZone: "UTC"` for every `@db.Date` (`formatDay`). **No date-fns** (struck from ARC-24, 2026-09-13). Rendered in the member's timezone (`Member.timezone` → tenant `ui.timezone` → Europe/Stockholm), tenant TZ for contacts; a member's "today" comes from that server-resolved zone, never the browser's. Weeks start on the tenant's `ui.weekStart` (default Monday) and are labelled by ISO week. `ui.showIsoWeek` governs only the due-date calendar's week column today — `/time`, `/time/team` and `/home` print the week number regardless (pre-existing, recorded in PLAN §0).
- Numbers: sv uses **decimal comma** and space thousands (`1 200,50 kr`); en uses `.`; input accepts both (`1,5` and `1.5` → 90 min).
- Duration style is a tenant preference `ui.durationStyle {hm, clock, decimal}` — `1h 30m` · `1:30:00` · `1,5 h` (clock style renders an estimate as `H:MM:SS` too — recorded, pre-existing); the same `<Duration>` component everywhere; raw seconds never rounded for display beyond the minute (2T), rounding only at invoice (Phase 4).
- Currency from `Project.billingCurrency`; never mixed in one total.
- Relative time ("2 h ago") only for activity/inbox; absolute elsewhere.

---

## 9. Accessibility

Testable rules. Every numeric claim below is asserted in `src/lib/contrast.test.ts`, which parses `src/app/globals.css` itself — the file that ships — so a lowered contrast fails CI rather than review. **When a row fails, the fix is the token, never the threshold.**

- Body and muted text ≥ 4.5:1 on every surface it renders on (`--background`, `--card`, `--popover`, `--muted`, `--sidebar`, `--accent`, and every tone tint). Placeholders included — SC 1.4.3 grants them no exemption.
- Control boundaries (`--input`, not `--border`) ≥ 3:1; outline-chip borders (`--tone-*-line`) ≥ 3:1 on the card they sit on.
- Focus indicator: `outline: 2px solid var(--ring)` at `outline-offset: 2px`, **never** a `box-shadow` ring (box-shadows are clipped by `overflow:hidden` ancestors — the table container, the tab strip, cards). Inside a scroll container use a negative offset (`-outline-offset-2`), as `TabNav` does. **A focusable table row is the one place the offset is `-6`, and it is keyed on `:focus`** (`TableRow`, 2026-09-16): measured inward from the row's border edge, the left edge is the 2px `visibilityRowCue` border, then the 2px `--primary` selected bar (an inset shadow), and only then the ring — at -2 the ring painted over the client-visible cue, at -4 over the selected bar; `:focus` because a row's focus only ever arrives by key, by the list's own `focus()` or by a deliberate click on its whitespace, and programmatic focus inherits the previous element's focus-visible state in Firefox and WebKit. Still an outline: forced-colors mode drops box-shadows, and engines differ on painting them on a collapsed-border row. **Photographed in Chromium only** (the harness has one project): whether WebKit paints an outline on a `<tr>` is unverified — a WebKit probe is owed, and if it fails there the ring moves to the row's first cell. Because the offset puts the ring on the *surface* behind the control, the gate measures ring × surface, not ring × control fill.
- Every drag has a keyboard twin (§7.1); every single-key action is also a menu item.
- Every icon-only control carries `aria-label` from `t()`, and its tooltip text is the identical string.
- Colour is never the sole carrier. Every enum value has a distinct icon silhouette; terminal values additionally strike their label; priority is geometry, not hue, at every level except URGENT.
- The active-row indicator is two channels everywhere — `--accent` fill **plus** a 2px `--primary` inset bar — in the table, the dropdown menu, the select and the command palette. An `--accent` fill alone measures ~1.07:1 against `--popover` and is not a focus indicator.
- Targets ≥ 24×24 CSS px; **grouped** icon buttons ≥ 28px (`size="icon-sm"` / `size="sm"`, SC 2.5.8 spacing clause). `size="xs"` (24px) is for isolated controls only.
- `prefers-reduced-motion: reduce` clamps every duration to **1ms — never `animation: none`**, because Radix exit transitions wait on `animationend` and would leave dialogs mounted for ever.
- `role="status"` on results, `role="alert"` on errors; `aria-invalid` helper text is announced, not merely tinted. `<FormMessage>` and `<Field>` do this for you.
- `aria-current="page"` on the active nav item, which also carries a 2px indicator and a colour change (three channels).
- Disabled state uses `--fg-disabled` / `--bg-disabled` + `cursor: not-allowed` (≥ 3:1), never `opacity: 0.5`.
- `color-scheme` is declared per theme so native scrollbars, date pickers and autofill follow.
- Both locales must pass at the longest Swedish string: chips and buttons wrap or grow, never truncate.

**The calendar grid** (`CalendarGrid`, `D`'s picker `footer`, 2026-09-13). A focused day handles exactly these keys:

| Key | Effect | `preventDefault` |
|---|---|---|
| `←` `→` / `↑` `↓` | ±1 day / ±7 days; the shown month follows | yes |
| `Home` / `End` | first / last day of the grid week under `ui.weekStart` | yes |
| `PageUp` / `PageDown` | ±1 month, the day clamped to the month's length | yes |
| `Shift+PageUp` / `Shift+PageDown` | ±1 year | yes |
| `Enter` / `Space` | not handled — the native button click commits that day | no |
| `Tab` / `Shift+Tab` | Radix's focus loop: search → previous month → next month → the one `tabIndex=0` day | no |
| `Esc` | never handled — Radix closes the picker; a second press closes the peek | no |
| any Ctrl/Meta/Alt chord, letters, `?` | not the grid's (`calendarMoveOf` → null), so ⌘K still reaches the dispatcher | no |

Nothing stops propagation. Focus moves one render after a keyboard move and never on mount (Radix's autofocus of the search field stands). Today is an underline plus `aria-current="date"`; the selected day is brand-tone fill, tone text, weight and a `--tone-brand-line` border, plus `aria-selected`. The line is the selection's only boundary of ≥ 3:1 (`contrast.test.ts` gates every `--tone-*-line` on `--popover`); the tint alone measures about 1.02:1 on the dark popover, fainter than the hover fill, so without it a hovered day read as the selected one. At the default hue that line is the ring's own indigo, so under `focus-visible` it turns transparent and the negative-offset ring lands wholly on `--tone-brand-bg` (gated ≥ 3:1) — otherwise the focused selected day, the first day Tab reaches whenever a date is set, read only as its border thickening by 1px. Never `bg-primary`, where the ring would vanish into the fill. Days outside 1970–2100 are inert spacers, not disabled buttons, and a row with no day in range (December 2100's sixth) is `aria-hidden` with no week number. The month buttons are `aria-disabled` at the bounds (a native `disabled` on the focused button would drop focus to `<body>`) and carry **no tooltip**, for the trigger's measured reason: a tooltip that opens on focus eats the next Escape.

---

## 10. The visual system (normative)

**Amended 2026-08-17.** This section replaces the ARC-15 sketch that preceded it. It is the reference for every new screen: **new UI is assembled from the tokens and components named here, and adds neither a colour nor a component of its own.**

### 10.1 Where the system lives

| concern | file |
|---|---|
| all tokens, utilities, base layer | `src/app/globals.css` |
| self-hosted faces + licences | `src/app/fonts/` (`inter-eu.woff2` 110 KB, `geistmono-eu.woff2` 28.5 KB, `build-fonts.mjs`) |
| entity colour algorithm | `src/lib/entity-color.ts` |
| tone → class strings | `src/lib/tones.ts` |
| enum → tone / icon / shape | `src/lib/enum-map.ts` |
| locale-correct numbers, money, durations, bytes | `src/lib/format.ts` |
| theme vocabulary, cookie, no-FOUC script | `src/lib/theme.ts` |
| **the release gate** | `src/lib/contrast.test.ts` (+ `color.ts`, `css-tokens.ts`) |
| the living preview | `/settings/design` |

Since set B: `src/components/semantic/timeline.tsx`, `progress-meter.tsx`, and `EntityTile` in `entity-chip.tsx`. Everything in `src/components/semantic/` is re-exported from `@/components/semantic` — **screens import from the barrel**, never from a deep path.

There is **no `tailwind.config.js`** — Tailwind 4 is CSS-first. Everything is `@theme` in `globals.css`.

### 10.2 Colour: three layers, in this order

1. **Primitives** in `@theme static` — the ramps. `@theme static` is required: without it Tailwind tree-shakes theme vars it cannot see used, and `--brand-h`, the surface ladder and the motion tokens vanish.
2. **Roles** in `:root` / `.dark` as plain custom properties — *not* in `@theme`.
3. **Bridge** in `@theme inline { --color-primary: var(--primary); … }`. Omitting `inline` makes dark mode silently do nothing.

Ramps: `slate` (neutral, carrying a hairline of brand chroma — zero-chroma grey is the signature of an untouched preset), `indigo` (brand, hue 268), `red` h25, `amber` h70, `green` h150. **No blue and no violet ramp**: info states use brand tints plus an info glyph. Surfaces are a separate ladder (`l0`–`l2`, `d0`–`d4`) so elevation is explicit rather than borrowed from the text ramp.

The brand accent has exactly **three jobs**: filled primary buttons, the active nav indicator, and the focus ring. Everything else is neutral chrome plus the tone set.

**Phase 7 seam.** Brand tokens are written `oklch(<L> <C> var(--brand-h, 268))`; a tenant override is one inline `style="--brand-h: 250"` on `<html>`. **Excluded from tenant control, and asserted so by the gate:** `--ring`, `--destructive`, `--success`, `--warning`, `--vis-*`, all `--chart-*`. A tenant whose hue is 25° must not make their primary button read as delete.

### 10.3 The semantic tone set — six tones

`neutral · brand · caution · success · danger · quiet`. Each is three tokens: `--tone-<t>-bg`, `--tone-<t>-fg`, `--tone-<t>-line`. A tinted chip is `bg` + `fg` (measures 6.3–7.9:1 light, 10.7–11.4:1 dark); an outline chip is transparent + `line` + `fg`. `quiet` is transparent with `--muted-foreground` and strikes its label; **`--tone-quiet-line` is `var(--input)`** so the quiet outline chip's hairline and the cancelled timeline node's glyph — the only marks those states carry — clear 3:1 instead of the 1.90:1 a decorative grey measured.

**Tone as plain text.** `--destructive` is a FILL colour: white on it measures 4.60:1, but *as text* on a dark card it measures 3.90:1 and fails SC 1.4.3. Danger text — `<FormMessage>`, `<Field error>`, the destructive menu item, the error toast icon — is therefore `--tone-danger-fg`, and every tone's `fg` is asserted ≥4.5:1 on card, canvas, muted, accent and popover. Reach for `--destructive` only where it is a fill or an outline.

Never reach for a tone by hand — `<StatusBadge>` picks it from `src/lib/enum-map.ts`.

### 10.4 The visibility system (safety-critical)

The worst bug this product can ship is a client seeing internal data. The two states differ on **five channels at once**, none of them hue alone:

| | Private to team | Client can see |
|---|---|---|
| fill | transparent | **solid warm** `--vis-client`, *identical in both themes* |
| icon | `lock`, outline | `eye`, filled |
| shape | 4px radius | full pill |
| weight | 500 | 600 |
| border | 1px `--input` | 1px `--vis-client-border` |
| row cue | 2px transparent left border (no layout shift) | 2px left border in `--vis-client-cue` via `visibilityRowCue()` |

Measured separation (client fill vs the card showing through the internal chip): **ΔE_OK 0.249–0.290 light, 0.543–0.593 dark**, across normal / protan / deutan / tritan.

Rules, all enforced in code:

- **Both states always render a chip.** Absence is not a state — absence is indistinguishable from a bug.
- The **fill** (`--vis-client`) is identical in both themes, because that is the pair the CVD gate measures. The **2px row cue** (`--vis-client-cue`) is not: the warm fill measures 2.10:1 on a white row, and a 2px edge at 2.1:1 is a decoration rather than a signal, so light draws the cue in the darker warm (≥4.19:1 on card, canvas, muted and row hover) and dark keeps the fill (≥6.49:1 on the same four). Never hand-write the class — call `visibilityRowCue()`, which the design gate itself renders.
- Never icon-only, at any density. Never optimistic — the item panel's `V` picker holds the chip on the canonical value until the server answers (`usePanelCommit`'s `optimistic: false`, slice 7), so a refused downgrade never flashes "Private to team" over a task the client can still see. *(The documents table's `VisibilitySelect` still shows the pick in flight — pre-existing, recorded in PLAN §0 with slice 7, not silently changed.)*
- The **write** control wears the same warm fill as the read chip when set to `CLIENT_VISIBLE` (`VisibilitySelect`, the upload form, the milestone row) — an editable row is never less legible than a read-only one.
- **Collision rule:** the warm band is the caution family, and a **filled warm pill means "Client can see" and nothing else, product-wide.** `warning` renders only as a tinted surface + 1px border + `triangle-alert` (`<Callout tone="caution">`), never as a filled pill. Project "Portal on" is `Badge variant="brand"` + `globe` for exactly this reason.

### 10.5 Enum → tone / icon / shape

The full table lives in `src/lib/enum-map.ts` (`STATUS_MAP`) and covers `clientStatus, projectStatus, milestoneStatus, versionStatus, approvalStatus, memberStatus, inviteStatus, serviceStatus, portalStatus, stateCategory, projectHealth, workItemKind, workItemType` plus `PRIORITIES`. It is the single source of truth: a screen never decides what colour `ACTIVE` is, so the same word cannot mean two things on two pages.

Labels come from `t("states.<domain>.<value>")` and are **never passed in** — a caller that could pass a label could pass a different word for the same state.

Deliberate: **priority is not hue-coded** across levels (geometry only; URGENT alone is red). **Health keeps RAG** because stakeholders read it that way, but always carries a distinct icon silhouette *and* its text.

### 10.6 Deterministic entity colour

Twelve frozen hues, each anchored to 3:1 against every surface of its theme, exposed as `--entity-0 … --entity-11`. `entityHash()` is FNV-1a 32-bit over the **immutable id** (`name` is a last-resort fallback for optimistic rows, reconciled on the server response — otherwise renaming a client would silently change its identity).

Entity colour is **identity, never status**; the label beside it always carries the meaning, and the tile is `aria-hidden`. Used by `<EntityChip>` (clients, projects) and `<MemberAvatar>` (people).

`<MemberAvatar>` deviates from the original spec on purpose: the spec coloured the *initials*, but the entity ramp is anchored to 3:1 — a non-text threshold — so coloured initials on a tint of their own hue measure ~3:1 and fail SC 1.4.3. The `.entity-tint` utility carries the identity (15% wash light, 28% dark) and the initials stay `--foreground`, measuring 16.8:1 light / 8.9:1 dark.

### 10.7 Typography

Self-hosted, subset, **zero network**: Inter 4.1 variable + Geist Mono 1.7.2 variable via `next/font/local`, two preloads, ~140 KB total, OFL licences committed beside them. Google's Inter build is stripped of `zero`, `ss01`–`ss08` and `cv01`–`cv14` — the exact features that disambiguate `ACME-12` — which alone forces a local file.

Root stays 16px; the `--text-*` keys are remapped so existing `text-sm`/`text-xs` sites moved with the system.

| role | utility | px | line | weight | tracking |
|---|---|---|---|---|---|
| display (auth) | `text-3xl` | 30 | 36 | 600 | −0.021em |
| dialog heading | `text-2xl` | 24 | 32 | 600 | −0.0195em |
| page title h1 | `text-xl` | 22 | 28 | 600 | −0.018em |
| section title h2 | `text-lg` | 16 | 22 | 600 | −0.011em |
| subsection h3 | `text-base` | 14 | 20 | 600 | −0.006em |
| body, table cell | `text-sm` | 13 | 20 | 400 | 0 |
| secondary / hint | `text-xs` | 12 | 16 | 400 | 0 |
| caption, badge, kbd | `text-2xs` | 11 | 14 | 500 | +0.005em |
| table header, eyebrow | `text-2xs uppercase` | 11 | 16 | 600 | +0.04em |

Weights are capped at **600**; 700 and 300 do not exist in this product. Hierarchy is size + colour, **three foreground tokens per view maximum** (`--foreground`, `--muted-foreground`, plus one tone). No italics.

`font-feature-settings` is declared **exactly once**, on `html` — it replaces rather than merges, so any second declaration silently drops `cv01`/`cv05`/`ss01`.

**Numerals.** `@utility num { font-variant-numeric: tabular-nums lining-nums slashed-zero }` goes on every numeric cell, duration, money value, count, byte size, version number, date, percentage, project key and `<kbd>`. Prose keeps proportional figures. Use `font-variant-numeric`, never `font-feature-settings`.

**Formatting** is `src/lib/format.ts` only — `Intl` formatters built once per locale in a module-level Map, never inside a render. sv-SE's group separator is U+00A0, so machine output uses `{useGrouping:false}` and client-side matching normalises it. Swedish runs 10–25% longer than English.

### 10.8 Space, shape, elevation

- **Spacing** on a 4px grid: `2 4 6 8 12 16 24 32 48`. The 2px step is only for icon-to-label gaps inside chips.
- **Control heights**: `xs 24` (isolated icon-only), `sm 28` (compact / **grouped** icon buttons), `default 32`, `lg 40` (prominent CTA).
- **Rows**: 36px default / 32px compact, set once by `<DataTable density>` as `--row-h` and consumed by both `TableRow` **and** `Skeleton`, so a table cannot load at one rhythm and settle at another. Table header 32px.
- **Radii**: `sm 4 · md 6 · lg 8 · xl 12`, plus `--radius-card: 10px`. Controls 6px, cards/dialogs/popovers 10px, badges/avatars `rounded-full`. **Nothing above 12px.**
- **One surface language: `1px solid var(--border)`.** Anchored things (cards, rows, sidebar, inputs, tables) get border + surface step, **never a shadow**. Exactly three shadows exist (`--shadow-1/2/3`) and only for things that genuinely float: dropdown/popover/tooltip/select, command palette/toast, dialog/sheet.
- **Content widths**: `--content-form` 720px (settings, account, auth), `--content-default` 1080px (detail pages), `--content-wide` 1440px (tables, board, backlog). Chosen via `<Page width>`.
- Card padding 16px (12px at `size="sm"`); 16px between cards, 24px between sections.

### 10.9 Motion

Tokens: `--dur-instant 80 · --dur-fast 120 · --dur-base 200 · --dur-slow 320`, with `--ease-out` / `--ease-entrance` / `--ease-exit`. Exits run at roughly two-thirds the enter duration.

**Animates:** colour on hover and press (80ms); popover, dropdown, tooltip and select opacity + 4px translate (120 enter / 80 exit); dialog and command palette (200 / 120); disclosure height; sheet transform (320 / 200); the toast stack.

**Never animates:** table row insert, remove or reorder; sort; column resize; numeric value changes; the board on data refresh (only on user drag); route changes; anything on an interval.

Never `transition-all` — enumerate the properties. Loading: under 200ms render nothing; 200ms–1s swap the trigger's leading icon for a spinner in a **fixed-width slot** so the button never resizes; over 1s show a skeleton matching the real row height and column widths.

### 10.10 Component catalogue

**Rule: a screen imports from `@/components/semantic` for anything that carries domain meaning, and from `@/components/ui` only for raw controls.**

`src/components/semantic/` — the layer that knows what a value *means*:

| component | use it for |
|---|---|
| `SectionCard` | **the one boxed surface.** Replaces every raw `<Card>` and every `rounded-md border border-border` wrapper |
| `DataTable` | the density/rhythm wrapper around every `<Table>` |
| `StatusBadge {domain, value}` | **every** enum value. Never a hand-rolled `<Badge variant={…}>` |
| `EntityChip {id, name, kind}` | every client/project reference in a list |
| `MemberAvatar {id, name}` | every person |
| `VisibilityBadge` + `visibilityRowCue()` | §10.4, always together on a row |
| `Callout {tone}` | every tinted notice block. Replaces the hand-rolled amber divs |
| `Field {label, htmlFor, hint, error}` | every labelled control (was duplicated three ways) |
| `Pending {label}` | the ellipsis indicator (was duplicated five ways) |
| `EmptyState {variant}` | `empty` vs `filtered` vs `forbidden` — three variants, never conflated. `empty` **requires** `icon` + `action` (§5.8) |
| `InlineEdit {kind,name,value,label}` | **every editable record property** (§5.11). Never a bare `<Input>` for a value that should read as text |
| `VisibilityInlineEdit` | the safety-critical one: rest state IS the badge (§10.4) |
| `PropertyPicker {open,value,options,onSelect,derive?,footer?}` | **every work-item property** (§5.2). Never an `InlineEdit` — that is for free text and record properties. *Recorded exception: the backlog table's S/P/E/D cells stay `InlineEdit` until a table-density trigger exists (§5.2)* |
| `CalendarGrid {value,today,weekStart,showWeekNumbers,onPick}` | the one month grid — only ever a `PropertyPicker` `footer`, never in a Sheet or Dialog (keyboard: §9) |
| `RowActions {label,primary,items}` | **every** per-row verb (§5.12). No destructive `<Button>` in a row, ever |
| `FileDropField` | the only file input in the product (§5.13) |
| `Disclosure {label}` | the one 28px progressive-disclosure trigger (§5.14) |
| `PageState {variant,icon,title,body,primary}` | 404, error boundary, unusable invitation (§5.15) |
| `Page {width}` / `PageHeader` | the content column and the h1 block |
| `Timeline` + `TimelineItem` | the one dated rail (§10.15) |
| `ProgressMeter {value,total,label}` | done-of-total, wherever a count has a denominator |
| `EntityTile {id,name,size}` | the identity mark alone, for an h1 that cannot be `EntityChip` |
| `MetricTile`, `HealthChip`, `PriorityIndicator`, `PriorityGlyph`, `KeyboardHint`, `ThemeToggle`, `StatusIcon` | as named — `PriorityGlyph` is the bars alone, for a row whose label already names the priority |

Utilities carry the roles that are typographic rather than componentised: `eyebrow` (the 11px/600/+0.04em uppercase table-header and eyebrow role — **never** written out as four classes), `otp-field` (a six-digit code), `row-h` (the `--row-h` rhythm outside a `<Table>`), `hairline-b`, `entity-tint`, and `hit` (an invisible 6px collar that lifts a 20px mark over the §9 touch floor **without moving a pixel of the design** — enlarge the hit area, never the glyph).

**Figures split by job.** `num` is tabular lining figures for a QUANTITY — money, counts, byte sizes, percentages, dates; `num-id` adds `slashed-zero` for an IDENTIFIER that is read character by character and transcribed — a project key, a version, a hash, an org. number, a postal code. The slash is a disambiguation device, and a slashed zero in a two-row count column at 13px reads as a rendering fault. In practice the rule is mechanical: wherever the mono face carries a code, it is `num-id`.

`src/components/ui/` — 25 primitives. Two are deliberately native: **`NativeSelect` and `NativeCheckbox` fire real `change` events**, which is what `<AutoForm>` listens for. The Radix `Checkbox` renders a button plus a bubble input and does **not** emit a bubbling change event — an auto-saving form built on it silently stops saving. Use the native pair inside `<AutoForm>`, Radix elsewhere.

### 10.11 Dark mode

Dark is a separate design, not an inversion.

1. **Elevation reverses** — surfaces get *lighter* with height (`d0` sidebar 0.115 → `d1` canvas 0.155 → `d2` card 0.205 → `d3` popover 0.245 → `d4` border/hover 0.300). The contract is **ΔL ≥ 0.04, not a contrast ratio** (card-over-canvas measures 1.10:1 — arithmetically nothing, perceptually obvious). Shipped steps: 0.040 / 0.050 / 0.040 / 0.055.
2. **Shadows stop doing work** — hairlines and surface steps carry elevation; the shadow tokens become pure black at higher alpha and are kept for dialogs only.
3. **Chroma drops**; the twelve entity dots are re-anchored to 3:1 against `--card`, not the canvas.
4. **Neither pure black nor pure white.**
5. **Borders are opaque** — never `oklch(1 0 0 / n%)`. Alpha borders cannot be statically contrast-tested and change meaning per backdrop; the gate asserts `alpha === 1`.
6. The sidebar **recedes** — darker than the canvas in *both* themes (asserted).
7. `color-scheme` is set per theme.

**The toggle is not `next-themes`.** The app already resolves per-request preferences server-side; a second client-only source of truth would drift from it and force a client boundary at the root. The preference lives in the `fl_theme` cookie (`SameSite=Lax`, one year), which becomes the mirror of the member preference row when that lands.

**No-FOUC contract, verified:** an explicit `light`/`dark` is server-rendered onto `<html>` and ships **zero** script; only `system` emits a ~180-byte synchronous script in `<head>`, before `<body>`, so it resolves pre-paint. `suppressHydrationWarning` on `<html>` only. `<meta name="theme-color">` cannot read a custom property, so its two literals live in `src/lib/theme.ts` and the gate asserts them equal to `--background` in each theme.

### 10.12 Density

Compact, information-dense, but calm — this is a tool people live in all day. 13px body on a 4px grid, 32px controls, 36px rows, 16px card padding, no zebra striping, no shadows on anchored elements, borders only.

Inside a row a control is 28px (`InlineEdit density="table"`, `RowActions`' ghost `icon-sm`, `MemberAvatar`). **`DataTable` sets `[&_td]:py-0.5`** — 2px, not the primitive's 6px — because a fixed-height row centres its cells, so vertical padding buys nothing visually and acts only as a floor that taller content pushes through. At 6px a 28px control made a 40px row and a 22px chip made a 34px compact row: the density promise broken by padding rather than by content. The audit measures every row's height against the `--row-h` it declares, so it cannot happen quietly again.

**A table that does not fit drops columns first, and whatever still overflows scrolls UNDER a pinned actions column.** `TableHead`/`TableCell` take `priority="high" | "medium" | "low" | "lower" | "lowest"`, and every rung is a width of the TABLE — `<DataTable>`'s scroll box is the `data-table` size container — never of the viewport: `medium` renders from a 38rem (608 px) box, `low` from 46rem (736), `lower` from 61.5rem (984), `lowest` from 71rem (1136); `high` always renders. The trailing actions column — a `<RowActions>`, or a row's lone verb such as the export list's download or the team table's statement link — is always `high` AND `pinned` (header and cells): `position: sticky` at the box's right edge, so a row's verbs are in view however far the table scrolls — column priority decides how much scrolls, pinning guarantees the verbs never do. **Why the table and not the viewport, measured 2026-09-16:** the rail is 224 px, open by default from `md` up and collapsible by the member, and a media query can see neither fact. The first implementation (`sm:`/`md:`) turned the low columns ON at 768 px, where the open rail leaves a 494 px box — narrower than `sm` — so ten tables overflowed there (a shared component counted once) and five put their row actions past the box (the documents table on all three files pages, members, invites, agreements, the backlog), and the backlog overflowed its box from 768 px all the way to ~1370 px, while the sweep's 1440 and 390 shots were both clean. **Choosing a rung.** Below `md` there is no rail, so `medium` and `low` are `sm` and `md` less the page's 16 px gutters: a phone keeps the columns it had (a flush table on the canvas to the pixel; a bordered or carded one reaches `medium` at 642 px). `lower` and `lowest` are for a table that needs more than a phone rung, placed for real laptops with a classic 17 px page scrollbar (which the harness's headless Chromium hides and a Windows browser does not — computed, not measured): a 1280 px window with the rail open (991 px) reaches `lower`; a 1440 px one with it open (1151) or a 1280 px one with it collapsed (1167) reaches `lowest`. A rung is measured, not guessed, and measured the way members see it — in SWEDISH, the product's first locale and the wider in every placeholder a table shows ("Ingen prioritet", "Ingen ansvarig", "Ange datum") — and the page's width caps the box: a `form` page never gives a table more than 670 px (so a column that must render there is `medium` at most — the rate cards' Scope on `/settings/rates` and the export list's Size were `low`, and never rendered there at all until the review caught it), a `default` page caps it at 1030 px. The backlog climbs all five (key · title · actions; + select, visibility; + state; + priority, assignee; + estimate, due), its min-content measured on the e2e seed at ~546 / 671 / 924 / 1099 px in English and ~556 / 669 / 958 / 1157 in Swedish (two builds, ±2 px — a HAND probe on a developer's machine, and the Swedish figure is now known to be low: CI measures the widest as ~1165), against rungs of 608 / 736 / 984 / 1136 — so in Swedish, at `lowest`'s very edge, the ten are wider than the box and scroll under the pinned column — **29 px at 1136 and 27 px at 1138, measured on CI run 35443979863** by the Swedish width walk (`e2e/zz-swedish-widths.spec.ts`), which ratchets them as the TRADE they are. The ~21 px this line carried before was that hand probe's arithmetic (1157 − 1136), and the runner disagrees by 8 px — which is the Windows-to-ubuntu font gap this document records everywhere else, arriving here too. The time week puts its agreement on `low` (at `medium` it was 29 px wider than a 608 px box, found by the sweep's first run at rung widths); **the time week's What cell takes a `min-w-40` floor with a `contain-inline-size` wrapper, and its three ADVISORY badges a `medium` rung** — the badges are `shrink-0` `whitespace-nowrap`, so four of them forced that column exactly as `/clients`' city span forced its own, and the table was 91 px past a 356 px box, 65 past 608 and 76 past 736 (CI run 35380530578), the largest overflow left in the product; ten rem and not the fourteen `/clients` uses because this table also carries a duration editor and a two-button pinned column, ~170 px of fixed width at a phone, so a 14 rem floor still left it 38 px over. **`Locked` takes no rung and no cap**: it is the one badge that explains a failed interaction — a locked row's editors are read-only and its ⋯ trigger renders disabled — so a phone that hid it would show a row that refuses to be touched and says nothing about why. A non-cell element that needs a rung takes it from `SHOW_FROM` in `table.tsx`, beside `PRIORITY`, so a re-measured rung cannot move for columns and not for them; **the team view's SHIFT DAY TOTALS puts its seven day columns on `low`** — a matrix cannot drop SOME of its columns, a week with holes being worse than no week, but it can drop all of them and leave the member and their total, which is the same answer the project month grid's week columns already gave; it was 249 px past a 356 px box and 111 px past the 494 px box the rail leaves at 768 px, the worst overflow in the product and unseen because that stop is exempt from the ratchet (2026-09-18); **the team view's TEAM HOURS contains BOTH its identity cells** — member and project each take a `contain-inline-size` wrapper over a floor, 6 rem and 9 rem, because a phone renders those two and Hours and nothing else, and each is unbounded tenant text in a `whitespace-nowrap` cell, so either alone is the column's floor: 13 px past a 356 px box and 0 at every other width (CI run 35400146183); containing only the wider one would have fitted that day's data and left a longer member name to put the scroll back, which an exempt stop never reports. SHIFT DAY TOTALS takes the same containment on its own member cell in the same change, and it sits at 0 px today only because this seed's names are short — the control case `/clients` and the client Projects tab already made in slice 28; at a 356 px box that table hides its seven day columns and the member name is what takes the remainder, and containment can only give width BACK (at the tightest box measured, 736 px, the seven days leave the member 113 px, its own min-content, which is why that floor is 6 rem too). **`/projects/[key]/money` takes the same answer in the one `lineTable` helper its four tables share**, whose label column is the only one without a width and labels a line "{key} {title}": 21 px past a 356 px box on "By epic" and "By task", 0 everywhere else and on "By member" and "By agreement", whose labels are merely SHORTER — the same latent floor, which is why the helper fixes all four. Ten rem there, per-table arithmetic again: at a 356 px box the value column is the only other one that renders, so the label column takes a 254 px remainder and the floor never binds — which leaves the ✦ revealed state, whose cost and margin columns are `high`, as the one place it can, and there a smaller floor is strictly less overflow. The client agreements table puts its rate on `low` (23 px too wide at `medium` in Swedish) and This month and Renews on `lower`. **Content moves a table's width.** A cell is `whitespace-nowrap`, so a column with tenant text in it grows with the longest value: the backlog's state and assignee names are capped at 6rem with `truncate` and the full text as the display's own `title` (a 29-character state widened its column by 134 px uncapped, 27 capped). What still grows — a long Swedish pair (~64 px past `lowest`'s edge), an 8-character project key with a four-digit number and a paperclip (+99 px on the key column), an uncapped group-header title — now costs a scroll, never a verb. **The pinned column, painted.** A sticky cell floats over the columns scrolling beneath it, so it is opaque and still reads as its row's: the cell takes the row's colour (`bg-inherit`), `::before` lays `--card` down, `::after` inherits the row's colour again over it — hover, an open menu, selection and a translucent row tint (the time week's running entry) all composite exactly as the row's other cells show them. The header cell is `bg-card` already and keeps its inset-shadow rule. A focusable row's ring (§9: a 2 px outline at -6 px) is painted with the row, under the pinned cell, so the cell draws the ring's top, right and bottom sides itself (a `pinned-ring` span, borders not a shadow, open on the left) — measured in the live page before it was built. `<ScrollFade>` sits just left of the pinned column, where the content now continues (`inset-y-px`, so a bordered table's hairline stays whole across it), and owns keyboard focus under the column, which the browser does not see covered: a control inside the box but under the column counts as visible and stays there (measured: the time week's duration editor sat 36 px under it — `scroll-padding-inline-end` did not move it, and was dropped, because it would also have counted the pinned column's own controls as hidden), so a `focusin` handler scrolls it out, a fade's width (32 px) short of the column while the table can still scroll right — only where the browser rings focus (`:focus-visible`: keyboard focus, and a text field however focused, so a mouse-opened `<InlineEdit>` field is revealed after its click has landed while a click on a resting control never moves it), only in a row that has a pinned cell, and never for a row itself or the pinned cell's own controls. **Covered since 2026-09-19**, in two halves that each catch what the other cannot: `src/lib/scroll-reveal.ts` holds the decision — `revealExempt` over all five exemptions and `revealScrollDelta` over the geometry — unit-tested across every combination, and `e2e/scroll-reveal.spec.ts` drives it in a browser, because a `node` suite has no layout and cannot say whether the component reads the right nodes. The e2e INJECTS the overflow (a `min-width` on the `<table>`), since slices 28-33 left no English stop that scrolls far enough for the reveal to bite; it walks a row with Tab and asserts at every stop that a focused control is never under the opaque column, and clear of the fade too while the table can still scroll right. Writing it found the fade's own observer watching `box.firstElementChild` — the `relative w-full` `table-container`, whose width IS the box's — instead of the `<table>`, so a table that grew AFTER first paint got no fade at all. Three more things the pinned layer covered, and how each was given back, all from the review: the cell holding focus lifts to `focus-within:z-3`, because on a 32px compact row a 28px ⋯ button's ring spills 2px into its neighbours and the next row's opaque cell painted over it; the scroll region's own focus ring is an overlay span above the pinned column (`data-table-focus`, `peer-focus-visible`), not the box's outline, whose right side the column covered; and the backlog's drag drop line is `z-2`, or it stopped short under the row's pinned cell. **The member's lever is the rail** on a page whose width the rail limits: at 1280 px collapsing it takes the backlog from 1008 px (eight columns) to 1184 (ten; 1167 with a scrollbar). A column with a priority renders only inside a `<DataTable>` — outside one there is no container and the query never matches. The scroll box that remains is `role="region" tabIndex={0}` with a real `aria-label` — a scroll container only a mouse can reach is content a keyboard user cannot read. **There is no second, stacked-row renderer for tables** (§12): one list implementation, columns removed by one prop. **The audit** asserts, on every stop, that every `[data-slot=row-actions]` sits in a cell whose COMPUTED position is sticky under a sticky header cell (`craft.unpinnedRowActions`, naming the table, so a lost class fails as surely as a forgotten prop — it cannot see a lone verb that is not a `<RowActions>`, which is why those two are named above) and that no row's actions lie outside its table's visible box (`offscreenRowActions` in `e2e/audit.ts`, recorded per width in the sweep's report) — at each device's own width on every walk AND, on the light desktop walk, at `RUNG_WIDTHS` (`e2e/visual.spec.ts`): 768 px, the narrowest box the rail leaves, and each rung's exact box with the rail open, for a flush table and for a bordered one. That walk is English with hidden scrollbars; **the Swedish margins are a walk of their own since 2026-09-19** (`e2e/zz-swedish-widths.spec.ts`, PLAN §0 owed (c)) — it switches the member's language, visits every authed stop at 390 px and all nine rung widths, and ratchets `tableOverflow` against its own CI-calibrated map while asserting the pin from the first run. It found the four documents tables far worse in Swedish than in English (14/14/14/7 px against 6/6/6/4 — the three `Filer` tables more than double, `/projects/[key]/files` 1.75x), a set of figures that existed nowhere before it. The scrollbar margins are still computed. And since the column is pinned, the ENGLISH walk — the subject again from here — proves the PIN at every rung, which stopped bounding how far a table overflows — so it now ALSO asserts `tableOverflow` (`e2e/audit.ts`), the box's own `scrollWidth − clientWidth`, at the same widths, against `KNOWN_OVERFLOW` in `e2e/visual.spec.ts`: a ratchet keyed `<stop>@<width>` holding every unlisted key to 0 and every listed one to the pixel measured on 2026-09-18. It found what the pin cannot — `/projects` 412 px over a 356 px box at 390 px (its `table-fixed min-w-3xl` was a hard 768 px floor: every number was exactly 768 − box) and `/clients` 149 px / `/clients?archived=1` 163 px (the name cell's `max-w-[420px]` is a floor in Chromium, a max-width clamping a cell's MIN-content contribution as well as its max-content one), both PLAN §0 slice-17 owed (f), and both worst on a PHONE, which this walk had photographed all along without asserting on it. **Both are fixed and their twelve keys deleted (2026-09-18, slice 28).** `/projects` keeps `table-fixed` — it is what aligns the six headings across the stacked per-client tables, since every column but the name carries its own width — and drops the floor, which a fixed table never needed: content cannot widen one, so the name column simply takes the remainder. That remainder is also why its Lead moved from `low` to `lower`: at a 736 px box six pinned widths left the name 67 px, three characters of a project name, and Lead is the widest and least identifying of the five, so it now waits for the 1280-px-with-rail box that can afford it (name 291 px at 736, 315 at 984). `/clients` and the client Projects tab drop the cap for the backlog's own answer — `contain-inline-size` + `w-full` on the cell's wrapper over a `min-w` floor — which takes the wrapper out of the column's intrinsic sizing entirely, so the slack spreads across all five columns instead of piling up behind the name (name 273 px at a 356 px box, 510 at 1151, 0 scroll at both; the cap never bound at 1151 anyway, where the column was 689 px). The floor is per table, not a constant: 14 rem on `/clients`, but 10 rem on the client Projects tab, whose 10ch key column made 14 rem 6 px too wide for a phone — measured, by the walk failing on it. Where the priorities alone are not enough, the identifying column takes a per-viewport cap (`max-w-28 sm:max-w-64` on `/files`, `max-w-36 sm:max-w-64` on `/members`) and truncates with a `title` — never the actions. Those caps are still VIEWPORT-scoped, an accepted exception: a cap only bounds a cell, the widest one applies from `sm` up, and 768 px with the rail open — the narrowest box that cap ever meets — is one of `RUNG_WIDTHS`.

### 10.13 The rule: tokens and components only

**New UI adds no colour and no component of its own.** If a screen needs a colour, it is already a token; if it needs a boxed surface, it is `SectionCard`; if it needs a status, it is `StatusBadge`.

Tripwires — each must return **nothing** across `src/app/**` and `src/components/**`:

```sh
# 1. raw palette utilities
grep -rEn "\b(bg|text|border|ring|fill|outline|divide|placeholder)-(neutral|gray|slate|zinc|stone|red|green|blue|amber|yellow|orange|purple|violet|indigo|teal|cyan|pink|rose|emerald|lime|sky)-[0-9]{2,3}" src/app src/components

# 2. colour literals (the only allowed pair lives in src/lib/theme.ts)
grep -rEn "#[0-9a-fA-F]{3,8}\b|oklch\(" src/app src/components --include=*.tsx

# 3. removed focus patterns
grep -rEn "ring-3|focus-visible:ring-\[|ring-ring/50|ring-1 ring-foreground" src/app src/components

# 4. dimming as a disabled state
grep -rEn "disabled:opacity-[0-9]{1,2}|[^-]opacity-50" src/app src/components --include=*.tsx

# 5. off-scale type
grep -rEn "text-\[[0-9]" src/app src/components --include=*.tsx

# 6. raw checkboxes (use NativeCheckbox inside AutoForm, Checkbox elsewhere)
grep -rn 'type="checkbox"' src/app src/components | grep -v native-checkbox

# 7. hand-written eyebrows and one-time-code fields (use the utilities)
grep -rEn "tracking-\[" src/app src/components --include=*.tsx

# 8. danger as TEXT (it is a fill colour; use --tone-danger-fg)
grep -rn "text-destructive" src/app src/components --include=*.tsx | grep -v "text-destructive-foreground"

# 9. a bordered DataTable nested inside a padded card (use flush).
#    The grep below only finds CORRECT usage, so it is a reading aid,
#    not the gate: the real check is the audit's `doubleHairlines`,
#    which measures the rendered borders and padding (§10.14).
grep -rn "rounded-none border-0" src/app | grep -i datatable

# 10. MANDATE 2 — a solid destructive FILL (the menu item is the correct
#     shape; the only legal fill is InlineConfirm's "Yes")
grep -rn 'variant="destructive"' src/app --include=*.tsx | grep -v DropdownMenuItem

# 11. MANDATE 3 — a file input outside FileDropField
grep -rn 'type="file"' src/app src/components --include=*.tsx | grep -v file-drop-field

# 12. MANDATE 1 — a bare control bound to a record property inside a
#     table row or a definition list (use InlineEdit)
grep -rEn "<(Input|NativeSelect|Textarea)[ />]" src/app --include=*.tsx | grep -iE "TableCell|<dd"

# 13. a second <details>/<summary> (use Disclosure)
grep -rn "<summary" src/app src/components --include=*.tsx | grep -v disclosure.tsx
```

Tripwire 10's one standing hit is `/settings/design`, the showcase whose job is to render every variant of every control; it is captioned as such. Tripwire 13's is nothing — `Disclosure` is the only `<summary>` in the product.

Justified standing exceptions, all inside `src/components/**`, none of them a colour: `KeyboardHint` (`h-[18px] min-w-[18px]`, `shadow-[0_1px_0_var(--input)]` — the specified kbd geometry), `PriorityIndicator` (`w-[3px] rounded-[1px]` — a sub-grid glyph), `EntityTile` (`text-[0.5625rem]` — 9px initials inside a 16px `aria-hidden` tile), `EmptyState` (`max-w-[340px]` — the specified measure), the `Tooltip` arrow geometry, the viewport-relative `Dialog`/`Sheet` widths, and the four `shadow-[inset_2px_0_0_var(--primary)]` active-row bars (a token reference; Tailwind has no inset-shadow utility). `Switch` uses `data-disabled:opacity-100` to *defeat* Radix's own dimming. `h-10` is the `lg` (40px) control height, not an ad-hoc value; `h-11`/`h-14` are the specified command-palette input row and mobile tab bar.

### 10.14 The release gate

`pnpm test` runs `src/lib/contrast.test.ts`, which parses `globals.css` and asserts the whole table in both themes: text ≥ 4.5:1 on every surface it renders on; non-text ≥ 3:1; tone chips, outline borders, visibility, entity dots, chart series and avatar washes; dark ΔL ≥ 0.04 and monotonically lighter; ΔE_OK ≥ 0.15 between the visibility fills and ≥ 0.10 between chart series under Machado 2009 severity-1.0 protan/deutan/tritan; no alpha borders; the brand seam excluded from `--ring`/`--destructive`/`--vis-*`; every colour role defined in **both** themes; and the `theme-color` literals equal to `--background`.

Set B added the pairs those screens actually paint: **every** tone (including `quiet`) as plain text and as a rule mark on card / canvas / muted / **accent** / popover; `--input` and `--ring` on `--accent`, because a control inside a table row is read on the HOVER surface and not on the resting one; `--destructive` as an outline; the internal chip's label and hairline on a hovered row; and the client-visible row cue at ≥3:1 on all five surfaces **plus** ΔE_OK ≥ 0.15 against the row it marks under all four vision types. Three tokens moved to satisfy those rows — dark `--input` 0.535 → 0.590, `--tone-quiet-line` → `var(--input)`, and the new `--vis-client-cue` — because the fix for a failing row is the token, never the threshold.

**No visual change ships on a screenshot of one page.** Every change to a token, a component or a screen is verified with the browser visual sweep — `pnpm test:e2e` (`e2e/visual.spec.ts`, `RUNBOOK.md` §7): 48 stops × light/dark × 1440×900 and 390×844 = 192 shots, each stop audited in the page for exactly one `h1`, text that composites to its own backdrop, untranslated message keys, horizontal overflow at phone width, broken images, and console/network errors. The shots land in `.design-shots/` (git-ignored) and are reviewed in both themes before the commit. A contrast table that passes proves the palette; only the sweep proves the pages.

**The craft audit** (`e2e/audit.ts`, run at all 192 stop/theme/device combinations — 48 stops × light/dark × desktop/phone) is the half of this document a screenshot cannot hold open. Each of these must be empty on every stop, or the run fails:

| it asserts | because |
|---|---|
| no visible `input`/`select`/`textarea` inside a resting `[data-slot=table-row]` (toggles excluded — a checkbox *is* its value) | MANDATE 1 |
| every `[data-slot=inline-edit]` is a `<button>` with a non-empty accessible name | §5.11 |
| no `[data-variant=destructive]` inside a table row, and no computed `--destructive` background on any control outside a `[role=group]` confirm | MANDATE 2 |
| `input[type=file]:not(.sr-only)` is empty | MANDATE 3 |
| every `[data-variant=empty]` contains a focusable descendant | §5.8 |
| every scrollable `[data-slot=data-table]` has `role=region`, `tabindex` and a non-empty `aria-label` | §10.12 |
| every `[data-slot=row-actions]` sits inside the visible box of its own table — never behind a horizontal scroll | §10.12 |
| no bordered `[data-slot=data-table]` inside a padded `[data-slot=section-card]` content box | §10.15.1 |
| every row's measured height equals its own `--row-h` (desktop) | §10.12 |
| exactly one `aria-current="page"` in the phone tab bar; the current tab is inside its strip's box | §3.3 |
| the h1 of `/files`, `/members`, `/settings/roles` does not contain the tenant name (the `<title>` may — today it is "{page} · Fortleva") | §10.7 |

Behavioural halves that a DOM audit cannot reach live in specs: `e2e/inline-edit.spec.ts` (a keyboard-only edit — Tab, Enter, type, Enter — persists across a reload; Escape sends **zero** `next-action` POSTs and returns focus to the trigger; a one-field edit leaves the other columns intact in the database) and `e2e/visibility.spec.ts` (the client-visible row cue survives `:last-child`; deleting a document takes two interactions and the first posts nothing).

One thing that gate deliberately does **not** assert: a `dueAt` is compared as a calendar DAY, not as an instant. The control is an `<input type="date">`, so the field's precision has always been the day, and the first save through the form normalises a seeded timestamp to midnight — the same thing the always-mounted date input did. Losing the day, or the visibility, is the erase H1 is about; losing a time-of-day the UI never showed is not.

`/settings/design` renders the same system live — the token ladder with computed ratios, every component state, all twelve entity colours, and the two visibility chips under protanopia / deuteranopia / tritanopia / greyscale filters. **If the two visibility chips are not instantly distinguishable in every panel, the pass is not done.**

### 10.15 Recurring screen patterns

Nine shapes recur across the app. A new screen picks one of them; it does not invent a tenth.

**1. List surface.** A page's primary table is a `<DataTable>` — which already *is* the card surface (1px hairline, 10px radius, `--card`). When the list needs a caption (a count, a second population on the same page, an inline create beneath), it goes inside a `<SectionCard title description>` with `contentClassName="p-0"` and `<DataTable flush>`, which drops the table's own hairline so the card's carries it — and adds the 16px first/last-column padding, or the table's content hangs 8px left of the title above it. Never nest a bordered `DataTable` inside a padded card: that draws two borders 16px apart, and the audit fails the run when it happens. Numeric columns are right-aligned `.num`; a project key column is `w-[10ch]` of the mono face marked `.num-id`; a byte size splits into `.num` value + muted unit (`bytesParts`). Columns carry `priority` (§10.12), and the row's verbs are a `<RowActions>` in the trailing, right-aligned, always-`high` column.

**2. Create-in-place.** A page that can create shows a `size="sm"` primary button in the `PageHeader` linking to `#new-<thing>`, the same button in its `EmptyState`, and an anchored `<SectionCard id="new-<thing>" className="scroll-mt-16">` at the foot. `/clients` and `/projects` are the reference; a modal is never used for creation (§12).

**3. Dated rail.** The `Timeline` is one column: a 24px node carrying the state's *icon* (silhouette first, tone second), a 1px `--border` rail that stops at the last node, and 16px between entries. Terminal states fill the node — except `quiet`, which is transparent by definition and would erase the ring, so a cancelled entry stays outlined and strikes its label. The rail and the node are `aria-hidden`; the state is repeated as a `StatusBadge` in the content, so nothing is carried by the decoration alone. `contentClassName` is where `visibilityRowCue()` goes. Used by the project timeline and by TOTP enrolment (three numbered steps).

**4. File visibility.** Every documents row says it three times: a `<VisibilityBadge>` — which is now literally the rest state of the editable control too (`VisibilityInlineEdit`, §5.11), so an editable row and a read-only row are the same silhouette — the 2px `visibilityRowCue()` on the row, and a legend strip *outside* the table hairline naming both states in words. The row also emits `data-visibility` for E2E. The upload form states where the file will land *before* it is sent: a standing info `Callout` for private, swapped for a caution naming who can open it the moment "Client can see" is chosen.

**The cue must survive the last row.** `TableBody` carries `[&_tr:last-child]:border-b-0`, **not** `border-0`: `border-0` zeroes all four widths, including the `border-left` the cue paints — which meant the last client-visible row of every table in the product silently lost its safety marking while the legend below it asserted it in words. `/settings/design`'s visibility demo puts a client-visible row **last** on purpose, so the showcase proves this rather than hiding it, and `e2e/visibility.spec.ts` asserts the computed `border-left-width` on **every** row of a real table — the last one included — plus that a client-visible row's `border-left-color` differs from an internal row's and is not alpha-0. *(Added 2026-09-02 with 2W-F slice 2. Until then this sentence described a guard that did not exist: the suite contained no `border` assertion at all. Nothing else can catch the trap — the craft audit's `rowPitch` measures row HEIGHT, which a missing left border does not change, so all 192 stops are blind to it.)*

**5. Permission matrix.** One scrolling region (`max-h-96`) with `position: sticky` module headers — it is a bare `fieldset`, not a nested `SectionCard`, because sticky is inert inside the card's `overflow-hidden`. Codes split `namespace:` + verb; the ✦ MFA marker is `role="img"` with an `aria-label`, not a bare span; tombstones are a caution `Callout` plus a per-row badge. A **system** role has no controls to label, so it renders as a tick/dash ledger with `sr-only` "granted"/"not granted" rather than 63 disabled checkboxes.

**6. The three empty states.** `empty` (nothing yet → create it), `filtered` (things exist, none match → clear the filter), `forbidden` (things exist, not for you). They are never conflated, and a whole-page denial is an `EmptyState variant="forbidden"` inside a `SectionCard` — never a `Callout`, which is for notices about content the reader *can* see. `empty` requires an icon and a verb (§5.8), so a tab whose feature has not shipped is **`forbidden`, not `empty`**: there is no verb to offer and inventing one would be a lie. It keeps its static, `aria-hidden` **structural preview** beside it under a "Preview" eyebrow, built from the real tokens and the real `--row-h` (board columns, backlog ghost rows), outline-only so it cannot be mistaken for a `<Skeleton>` that is about to resolve. Never an illustration.

**7. The read-first property list.** A card of labelled properties is a `<dl>`: `<dt>` the label at `text-xs text-muted-foreground`, `<dd>` an `<InlineEdit>` (§5.11). The label carries the control's own `px-2.5` inset so the resting text sits under its label rather than 10px right of it. **Only populated fields render at rest**; everything blank waits behind one `<Disclosure label={t("addDetails")}>`, so the card states what is true instead of advertising ten blanks. The hidden inputs inside the disclosure still post, open or closed. Lead with the fact that identifies the record, not with the name the h1 already carries. **The item rail is the side-by-side variant** (label and value in two grid columns): every row is at least the `PropertyPicker` trigger's height, `min-h-8` — text labels and values padded (`py-1.5`, one 20px line to 32px, so a wrapping value keeps its label level with its first line), picker values centred in the same 32px. Mixing 32px triggers with 20px text lines put each picker's label 6px above its value; the sweep's screenshots showed it, and no assertion did (2026-09-13).

**8. The row's actions.** The trailing cell is right-aligned and holds at most one ghost icon plus a `⋯` (§5.12). A page-level destructive is not a row action: it is a danger-footer `SectionCard` at the foot of the main column (§5.9).

**9. The whole-page state.** `<PageState>` (§5.15). Four routes, one shape, one h1 size, one left edge, always a way forward.

---

## 11. Portal-specific rules

| Never shown to a Contact | Shown to a Contact |
|---|---|
| State names, estimates, priorities, labels, links, member names / avatars (v1), rates, cost, per-member time, INTERNAL activity, internal notes, repo/hosting fields, unpublished updates | Categories (Planned / In progress / Done), milestones + progress bar, published updates with health, timeline, files & deliverables, action items, own requests, hours summary (opt-in, PRIMARY only) |

- No board unless the project enables it; then it is the same `<WorkItemView>` with category columns and no drag.
- Portal reads use only `modules/*/portal.ts` projections; **View-as-Contact** in the member app renders through the very same functions (banner "Viewing as <contact> — you see exactly what they see").
- Portal chrome is minimal: tenant name/logo, project switcher, profile menu. No ⌘K, no keymap, no timer.
- Copy is written for a business reader: no "backlog", "triage", "epic" — say "Requested", "In progress", "Phase".
- Every portal write is one of the eight actions in §4; the UI offers nothing else.

---

## 12. What we deliberately don't do (UI)

- Modals for creating tasks, time entries, or comments; wizard flows for everyday work.
- Save buttons; unsaved-changes prompts.
- Hidden or implicit filters; "smart" views the user cannot see the definition of.
- Silent archive/disappearance (ADO 183-day style); every archive is an explicit, reversible action with a "show older" path.
- Dashboard/report builders, card style rules, per-team board settings, swimlane editors, custom column fields.
- Presence indicators, "who is online", activity feeds of colleagues, leaderboards, heatmaps, per-minute timelines — the never-list (SECURITY.md).
- Multi-assignee pickers, sprint pickers by default, story points by default.
- Portal features that require learning: no kanban by default, no state names, no filters beyond "open / done", no public links.
- A second implementation of any list, board, comment thread, attachment list, notification list, or search box.
